name: Build Windows (native)

on:
  workflow_dispatch:

jobs:
  build-win:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: Topics/JumpKit/app/package-lock.json
      - name: Install deps
        run: npm ci
        working-directory: Topics/JumpKit/app
      - name: Rebuild native for Electron (Windows)
        run: npx electron-builder install-app-deps
        working-directory: Topics/JumpKit/app
      - name: Build Windows installer
        run: npm run build:win
        working-directory: Topics/JumpKit/app
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: jumpkit-win
          path: |
            Topics/JumpKit/app/dist/JumpKit-Setup-*-Prod-x64.exe
            Topics/JumpKit/app/dist/JumpKit-Setup-*-Prod-x64.exe.blockmap
            Topics/JumpKit/app/dist/latest.yml
          if-no-files-found: error
