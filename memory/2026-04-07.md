# 2026-04-07 — Session Notes

## 📋 JumpKit To-Do List (as of Apr 10)

1. **Audit tests 46–51** — verify team sharing writes to Supabase + local SQLite are in sync
2. **Live invite email test** — trigger a real team invite, verify end-to-end delivery via Resend
3. **Account + Teams page UI cleanup** — review layout/design, evaluate merging into one page
4. **Supademo** — Jeff records demo, Max swaps into landing page in place of static screenshots
5. **Connect "Get JumpKit Now" landing page buttons to Lemon Squeezy** — free tier card button still links to `#` (download placeholder), needs to be wired to actual download or LS flow
6. **Security + vibe coding audit + backup** — check for security issues and vibe coding errors, then backup

### ✅ Done
- Tier-based paywall gating: Jet AI (teams_jet only) + Teams (core+) ✅ Apr 10
- Unit tests 52–54 for paywall gating ✅ Apr 10



## JumpKit X / Social
- Created @JumpKitAi X handle
- First post live: "Your bookmarks are a mess..." with JumpKit logo image
- Built list of accounts to follow (AI, productivity, indie SaaS spaces)
- Discussed Supademo as replacement for static landing page screenshots — Jeff will record demo and ping Max to do the swap

## Landing Page — CTA Email Signup
- Replaced "Get JumpKit Now" button in "Ready to stop searching?" section with email signup form
- Styles were already in place (`.cta-form`, `.cta-input`, `.cta-note`)
- Form shows "Signing up..." while submitting, success/error state after

## Waitlist — Full Pipeline Live ✅
- Created `waitlist` Supabase table (email unique, created_at)
- Built + deployed `waitlist-signup` Edge Function (Supabase + Resend)
  - Stores email via service role key (upsert, ignores duplicates)
  - Sends confirmation email from noreply@jumpkit.ai via Resend
  - CORS handled for browser fetch
- Wired `handleSubmit` in landing/script.js to hit edge function
- Fixed logo image in confirmation email (width: auto to prevent stretching)
- Tested end-to-end — confirmed working ✅

## Git
- Landing: commits c57e220 (email form wire-up) pushed to GitHub
- App: commit fb30a50 (waitlist edge function)
- Vercel redeployed manually by Jeff

## Domain Update (Apr 18)
- jumpkit.app is now the primary domain
- jumpkit.ai 301 redirects to jumpkit.app (both apex + www)
- All jumpkit.ai URLs in app code updated to jumpkit.app (commit 0835ba9)

## jumpkit.app DNS (May 2)
- jumpkit.app now renders the landing page ✅
- Vercel still shows "invalid configuration" — likely SSL cert pending
- All 3 A records added to GoDaddy: 15.197.225.128, 3.33.251.168, 216.198.79.1
- TODO: check Vercel cert status tomorrow, click Refresh to force re-check
