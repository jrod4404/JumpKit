# 2026-06-18 — Tests 93 & 130 + Auth Fixes + Deployment Page + Deployments Table

## Tests Run Today
- **Test 93** — LS webhook upgrades existing account → PASSED ✅
- **Test 130** — apply-pending-upgrade (paid before account creation) → PASSED ✅

## Auth Improvements (auth.js + landing)
- Login shows "Click here to resend the confirmation email" on email-not-confirmed error
- Resend calls `supabaseClient.auth.resend()` with `emailRedirectTo: https://jumpkit.app/confirmed`
- `confirmed.html` shows red "Link Expired" card on `#error=otp_expired`
- `index.html` root detects error hash → red overlay modal; detects `access_token+type=signup` → redirects to `/confirmed`
- Vercel root directory fixed to `Topics/JumpKit/landing` → GitHub pushes now auto-deploy

## Pre-Flight Code Audit Tests Added (375-378)
- 375: Database Security & Integrity Audit
- 376: localStorage/sessionStorage Audit (approved key inventory)
- 377: Code Quality Audit (console.logs, async errors, UI states, pagination)
- 378: Full Security Audit (secrets, Electron hardening, XSS, auth, API, supply chain, privacy)
- Pre-flight filter refactored from hardcoded `id===139` to `preflight:true` flag

## Deployment Page (new)
- New "Deployment" admin page with world-upload icon in sidebar
- 5 phases (Pre-Deploy + Testing removed, remaining: Code & Version, Backup, Build Installers, Landing, Release)
- Section headers match tests page style (flat header, collapsed by default, inline pills)
- Done/To Do toggle buttons, green bg on done rows matching tests pass row style
- Summary card (Done/To Do/Total) in fixed header bar with border-bottom
- Reset All + Manage Deployment + Save Results buttons
- State persisted in `localStorage` as `jk_deploy_state`
- Deploy page: `_loadedScripts.delete()` before load so always fresh

## Deployments Supabase Table (new)
Migration: `supabase/migrations/20260618000000_create_deployments.sql`
Fields: id, created_at, version, testing_account, testing_completed_at, test_os, tests_total/passed/failed/skipped, deployment_folder, mac_results_file, win_results_file, status, commit_id, deployed_at, deploy_account, mac_installer_path, win_installer_path, deploy_results_file, notes
RLS: admin-only via profiles.role = 'admin'

## Manage Testing Modal (tests page)
- Added Version Number field
- Added Deployment Folder field (Choose button → directory picker)
- Saves version + folder to shared `jk_deploy_config` localStorage key
- "Finalize Testing & Prepare Deployment" button (renamed from "Conclude & Reset", blue)
- On click: inserts row to Supabase `deployments` table with full test scorecard
- Auto-copies test result HTML files to deployment folder on each Save Results

## Manage Deployment Modal (deploy page)
- Dropdown populated from Supabase `deployments` table
- Shows version, date, account per row
- On selection: auto-applies version + deployment_folder to deploy page
- Mac/Win installer path fields + Notes
- "Finalize Deployment" button → auto-fetches git commit ID via IPC → confirmation modal → updates deployments row with commit_id, deployed_at, deploy_account, status='deployed'

## Electron IPC Added
- `getLatestCommitId`: runs `git log --oneline -1` in repo root → returns `{commitId, message}`
- `openFileDialog` updated to support `properties:['openDirectory']` via opts

## Renamed
- "Start, Stop & Manage Testing" → "Manage Testing" (all occurrences)
- "Conclude & Reset" → "Finalize Testing & Prepare Deployment"

## Vercel Auth Issue
- Fixed via `gh auth login --web` then `gh auth setup-git`
- Wired GitHub CLI token into git credential helper → no more auth prompts

## Commits Today (key ones)
- a73bdd1 — auth confirmation flow fixes + test 93/130 step updates
- dcfcde2 — landing page error hash overlay
- 40cb24b — emailRedirectTo in resend + signup token redirect
- 8f6f174 — remove console.debug from user ID migration
- 2e79381 — pre-flight test 375 (DB audit)
- f7268b0 — pre-flight test 376 (localStorage audit)
- 8cb5ab7 — pre-flight test 377 (code quality audit)
- 70a868f — pre-flight test 378 (security audit)
- c44fad3 — Deployment page created
- 1965cc6 — deployments table + finalize testing/deployment flow
# 2026-06-18 — Tests 93 & 130 + Auth Fixes + Deployment Page + Deployments Table

## Tests Run Today
- **Test 93** — LS webhook upgrades existing account → PASSED ✅
- **Test 130** — apply-pending-upgrade (paid before account creation) → PASSED ✅

## Auth Improvements (auth.js + landing)
- Login shows "Click here to resend the confirmation email" on email-not-confirmed error
- Resend calls `supabaseClient.auth.resend()` with `emailRedirectTo: https://jumpkit.app/confirmed`
- `confirmed.html` shows red "Link Expired" card on `#error=otp_expired`
- `index.html` root detects error hash → red overlay modal; detects `access_token+type=signup` → redirects to `/confirmed`
- Vercel root directory fixed to `Topics/JumpKit/landing` → GitHub pushes now auto-deploy

## Pre-Flight Code Audit Tests Added (375-378)
- 375: Database Security & Integrity Audit
- 376: localStorage/sessionStorage Audit (approved key inventory)
- 377: Code Quality Audit (console.logs, async errors, UI states, pagination)
- 378: Full Security Audit (secrets, Electron hardening, XSS, auth, API, supply chain, privacy)
- Pre-flight filter refactored from hardcoded `id===139` to `preflight:true` flag

## Deployment Page (new)
- New "Deployment" admin page with world-upload icon in sidebar
- 5 phases (Pre-Deploy + Testing removed, remaining: Code & Version, Backup, Build Installers, Landing, Release)
- Section headers match tests page style (flat header, collapsed by default, inline pills)
- Done/To Do toggle buttons, green bg on done rows matching tests pass row style
- Summary card (Done/To Do/Total) in fixed header bar with border-bottom
- Reset All + Manage Deployment + Save Results buttons
- State persisted in `localStorage` as `jk_deploy_state`
- Deploy page: `_loadedScripts.delete()` before load so always fresh

## Deployments Supabase Table (new)
Migration: `supabase/migrations/20260618000000_create_deployments.sql`
Fields: id, created_at, version, testing_account, testing_completed_at, test_os, tests_total/passed/failed/skipped, deployment_folder, mac_results_file, win_results_file, status, commit_id, deployed_at, deploy_account, mac_installer_path, win_installer_path, deploy_results_file, notes
RLS: admin-only via profiles.role = 'admin'

## Manage Testing Modal (tests page)
- Added Version Number field
- Added Deployment Folder field (Choose button → directory picker)
- Saves version + folder to shared `jk_deploy_config` localStorage key
- "Finalize Testing & Prepare Deployment" button (renamed from "Conclude & Reset", blue)
- On click: inserts row to Supabase `deployments` table with full test scorecard
- Auto-copies test result HTML files to deployment folder on each Save Results

## Manage Deployment Modal (deploy page)
- Dropdown populated from Supabase `deployments` table
- Shows version, date, account per row
- On selection: auto-applies version + deployment_folder to deploy page
- Mac/Win installer path fields + Notes
- "Finalize Deployment" button → auto-fetches git commit ID via IPC → confirmation modal → updates deployments row with commit_id, deployed_at, deploy_account, status='deployed'

## Electron IPC Added
- `getLatestCommitId`: runs `git log --oneline -1` in repo root → returns `{commitId, message}`
- `openFileDialog` updated to support `properties:['openDirectory']` via opts

## Renamed
- "Start, Stop & Manage Testing" → "Manage Testing" (all occurrences)
- "Conclude & Reset" → "Finalize Testing & Prepare Deployment"

## Vercel Auth Issue
- Fixed via `gh auth login --web` then `gh auth setup-git`
- Wired GitHub CLI token into git credential helper → no more auth prompts

## Commits Today (key ones)
- a73bdd1 — auth confirmation flow fixes + test 93/130 step updates
- dcfcde2 — landing page error hash overlay
- 40cb24b — emailRedirectTo in resend + signup token redirect
- 8f6f174 — remove console.debug from user ID migration
- 2e79381 — pre-flight test 375 (DB audit)
- f7268b0 — pre-flight test 376 (localStorage audit)
- 8cb5ab7 — pre-flight test 377 (code quality audit)
- 70a868f — pre-flight test 378 (security audit)
- c44fad3 — Deployment page created
- 1965cc6 — deployments table + finalize testing/deployment flow

## Open TODOs (end of session)
- [ ] Run migration `supabase/migrations/20260618000000_create_deployments.sql` in Supabase SQL Editor
- [ ] Verify Supabase RLS roles (admin requirement) work in production
- [ ] Deploy Checklist modal (`_openDeployChecklistModal`) still needs full implementation
- [ ] Confirm deployment HTML export paths align with selected deployment metadata in real environment
- [ ] Icon colors on deploy page: color icons same as section header text color (requested but session ended)
