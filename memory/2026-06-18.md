# 2026-06-18 — Tests 93 & 130 + Auth Flow Fixes

## Tests Run Today
- **Test 93** — LS webhook upgrades existing account → PASSED ✅
- **Test 130** — apply-pending-upgrade (paid before account creation) → PASSED ✅

## Code Changes (all committed + pushed)

### tests.js
- Added `link: { url, label }` support to inline step renderer (renders turquoise external-link icon)
- Test 93 steps overhauled: LS checkout link on step 1 (orange banner note), Supabase profile check step, "free account from step 2" wording, JumpKit Unlimited modal check in step 6
- Test 130 step 1 synced with test 93
- LS checkout URL updated to full buy link in both tests:
  `https://jumpkit.lemonsqueezy.com/checkout/buy/81c37b98-510a-4ca9-9849-06f10fd3a8d0`

### auth.js
- "Email not confirmed" login error now shows inline resend link ("Click here to resend the confirmation email")
- Resend calls `supabaseClient.auth.resend({ type: 'signup', email, options: { emailRedirectTo: 'https://jumpkit.app/confirmed' } })`
- Success/fail feedback on resend (green alert or error message)
- 429 rate limit handled gracefully

### landing/confirmed.html
- Added hash detection script: reads `#error=` on load
- `otp_expired` → shows red "Link Expired" card with resend instructions
- Other errors → generic "Confirmation Failed" message

### landing/index.html
- Added hash detection script on landing root
- `#error=otp_expired` → shows red overlay modal (Supabase sends errors to Site URL, not /confirmed)
- `#access_token + type=signup` → auto-redirects to `/confirmed` (handles resend landing on root)

## Vercel Fix
- Root Directory was misconfigured — set to `Topics/JumpKit/landing`
- GitHub pushes now auto-deploy landing page correctly
- Had to manually alias `jumpkit.app` to new deployment once to get it working
- Future pushes: commit → push → Vercel auto-deploys ✅

## Supabase Notes
- `https://jumpkit.app/confirmed` already in Supabase allowed redirect URLs
- Supabase sends error conditions (expired OTP etc.) to Site URL root, not redirect_to
- OTP expiry default ~1hr — if testing with suppressed emails, token may expire before you click
- Resend rate limit: ~60s between resend attempts per email (429 handled gracefully)
- Email suppression: found help@jumpkit.app was suppressed in Resend due to prior bounce — fixed via Resend dashboard → email → hover Suppressed → Remove

## Commits Today
- a73bdd1 — feat: resend confirm link, expired OTP error page, test 93/130 step updates
- dcfcde2 — fix: detect Supabase auth error hash on landing page root and show overlay
- 40cb24b — fix: pass emailRedirectTo in resend call; redirect signup token from root to /confirmed
# 2026-06-18 — Tests 93 & 130 + Auth Flow Fixes + Pre-Flight Tests 375-377

## Tests Run Today
- **Test 93** — LS webhook upgrades existing account → PASSED ✅
- **Test 130** — apply-pending-upgrade (paid before account creation) → PASSED ✅

## Code Changes (all committed + pushed)

### tests.js
- Added `link: { url, label }` support to inline step renderer (renders turquoise external-link icon)
- Test 93 steps overhauled: LS checkout link on step 1 (orange banner note), Supabase profile check step, "free account from step 2" wording, JumpKit Unlimited modal check in step 6
- Test 130 step 1 synced with test 93
- LS checkout URL updated to full buy link in both tests:
  `https://jumpkit.lemonsqueezy.com/checkout/buy/81c37b98-510a-4ca9-9849-06f10fd3a8d0`
- Pre-flight section filter refactored to `preflight: true` flag
- **Test 375** added — Database Security & Integrity Audit (copyable prompt, 16 checks)
- **Test 376** added — localStorage/sessionStorage Audit (copyable prompt, approved key inventory)
- **Test 377** added — Code Quality Audit (copyable prompt, 15+ checks)

### auth.js
- "Email not confirmed" login error now shows inline resend link ("Click here to resend the confirmation email")
- Resend calls `supabaseClient.auth.resend({ type: 'signup', email, options: { emailRedirectTo: 'https://jumpkit.app/confirmed' } })`
- Success/fail feedback on resend (green alert or error message)
- 429 rate limit handled gracefully
- Removed `console.debug` from local user ID migration (was logging on first login per device)

### landing/confirmed.html
- Added hash detection script: reads `#error=` on load
- `otp_expired` → shows red "Link Expired" card with resend instructions
- Other errors → generic "Confirmation Failed" message

### landing/index.html
- Added hash detection script on landing root
- `#error=otp_expired` → shows red overlay modal (Supabase sends errors to Site URL, not /confirmed)
- `#access_token + type=signup` → auto-redirects to `/confirmed` (handles resend landing on root)

## Vercel Fix
- Root Directory was misconfigured — set to `Topics/JumpKit/landing`
- GitHub pushes now auto-deploy landing page correctly
- Had to manually alias `jumpkit.app` to new deployment once to get it working
- Future pushes: commit → push → Vercel auto-deploys ✅
- Root Directory setting is under Build & Development Settings (not General)

## Supabase Notes
- `https://jumpkit.app/confirmed` already in Supabase allowed redirect URLs
- Supabase sends error conditions (expired OTP etc.) to Site URL root, not redirect_to
- OTP expiry default ~1hr — if testing with suppressed emails, token may expire before you click
- Resend rate limit: ~60s between resend attempts per email (429 handled gracefully)
- Email suppression: found help@jumpkit.app was suppressed in Resend due to prior bounce — fixed via Resend dashboard → email → hover Suppressed → Remove

## Pre-Flight Audit Tests Added (375-377)
All three are in the Pre-Flight section with copyable prompts:
- **375** — Database Security & Integrity Audit (SQLite parameterization, RLS, service role key, migrations, dev/prod separation)
- **376** — localStorage/sessionStorage Audit (approved key inventory: 13 LS keys + 2 sessionStorage keys)
- **377** — Code Quality Audit (console.logs, unhandled async, missing UI states, unbounded queries, XSS, event leaks)

### Approved localStorage keys (as of today)
`jk_users`, `jk_jumps_{userId}`, `jk_cols_{userId}`, `jk_clicks_{userId}`, `jk_prefs_{userId}`, `jk_backup_reminder_ts`, `jk_license_notif_ts`, `jk_notified_invite_ids`, `jk_sidebar_collapsed`, `jk_sync_fail_notif_ts`, `jk_teams_expanded`, `jk_theme`, `jk_trial_notif_milestone`

### Approved sessionStorage keys
`jk_pending_upgrade_applied`, `jk_session_token`
