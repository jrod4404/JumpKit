# Memory — 2026-06-08

## JumpKit Settings Page — Major Overhaul Session

Long morning session of Settings page polish and bug fixes. All changes committed to git.

### Settings Page Changes
- **Labels updated:**
  - "Auto Backup" → "Auto-Backup Jumps"
  - "Backup Now" → "Backup Jumps Manually"
  - "Display hotkey pill on jump cards" → "Show or hide hotkey pill next to jump name on jumps page"
  - "Display description under jump name" → "Show or hide description under jump name on jump page"
  - "Seconds saved per jump launch" → "The default seconds saved per jump launch, set to what is accurate for you"
  - "In-app notification alerts" → "Show or mute in-app notification alerts"
  - "Used to calculate ROI" → "Your personal dollars per hour, used to calculate ROI, set to what is accurate for you"
  - "Export all data to JSON file" → "Export all data to a local JSON file"
  - "/hr" → "/ hr" on dollars per hour field
  - "Productivity" card renamed → **ROI** (with `ti-chart-bar` icon)

- **Settings card structure:**
  - **Preferences:** Starting Page, Nav Menu on Startup, Notifications, Show Jump Description, Show Hotkey (description + hotkey moved to bottom)
  - **ROI:** Time Saved per Click, Dollars per Hour
  - **Maintenance:** Backup Jumps Manually, Auto-Backup Jumps, Auto-Archive Jumps (in this order)

- **Default dollars per hour:** changed from 150 → **100** (updated in both `db.js` and `app.js` fallback)

- **Starting page dropdown:** added Teams, Settings, My Account as options; dropdown set to `max-height: none` to remove scrollbar

- **Account page tab toggler:** reordered to Teams | Settings | My Account; "My Teams" → "Teams"

### Unlimited Only Pill & Upgrade Button
- Removed `!` from all "Unlock JumpKit Unlimited" CTA buttons throughout the app
- "Unlimited only" indicator changed to a styled pill with subtle turquoise border/background + `ti-lock` SVG icon (not emoji), icon size `1rem`
- Pill and Upgrade button now same height via `align-items: stretch`
- Paywall modals for Auto-Archive and Auto-Backup now include 1-sentence feature descriptions

### Bug Fixes
- **Double-click modal close bug:** Root cause was duplicate `document.addEventListener('click')` handlers in both `app.js` and `jumps.js` for `show-upgrade-modal` and `modal-close`. Removed duplicates from `jumps.js` — `app.js` now owns all global modal actions.

### Sidebar + Page Title Sync
- Clicking Teams tab → sidebar highlights Teams, title = "Teams"
- Clicking Settings tab → sidebar highlights Settings, title = "Settings"
- Clicking My Account tab → **nothing highlighted** in sidebar, title = "My Account"

### Auto-Archive & Auto-Backup (Free Tier Paywall)
- Both features locked for free users with pill + Upgrade button
- Descriptions visible to all users (not just Unlimited)
- Paywall modals describe what each feature does

### TODO (still pending from list)
- Test settings UI in app (#3 test checklist provided to Jeff)
- Test 5-member free-tier team cap (#4 test checklist provided to Jeff)
