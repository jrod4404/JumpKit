
---

## JumpKit Session — 2026-06-13 Morning

### Pending Upgrade Flow (Pay Before Account) — FULLY TESTED ✅
- `pending_upgrades` table stores tier + ls_customer_id when webhook fires with no matching profile
- `ls-webhook` redeployed with `--no-verify-jwt` (was returning 401 to LS)
- `apply-pending-upgrade` edge function: applies tier on login, deletes row, fires send-welcome-core
- **Bug fixed:** was fire-and-forget; changed to `await` in auth.js so upgrade applies before redirect to app.html
- **Bug fixed:** modal not showing on re-test — used `sessionStorage.setItem('jk_pending_upgrade_applied', '1')` in auth.js, checked in app.js to reliably show modal regardless of `last_known_tier`

### New: send-pending-upgrade Edge Function ✅
- Fires from `ls-webhook` in the 0-row case (user paid, no account yet)
- Sends branded onboarding email: "Your JumpKit Unlimited subscription is confirmed ⚡"
- 3-step getting started: Download (Mac/Win links) → Create account with same email → Login (auto-applies)
- Email template: `app/emails/pending-upgrade-email.html`
- Edge function: `app/supabase/functions/send-pending-upgrade/index.ts`
- Deployed with `--no-verify-jwt`
- Tested and confirmed ✅

### Welcome to JumpKit Unlimited Modal Redesign
- Replaced comparison grid (Free vs Unlimited) with:
  - Lightning bolt icon in modal title (teal)
  - Gradient teal/blue confirmation banner with checkmark circle
  - Feature checklist with gradient circle checkmarks
  - Full-width gradient "Let's Go →" CTA button

### Home Page Fixes
- No-teams card: added `margin-bottom: 20px` to match `.home-teams-grid` spacing
- Account/stats stat-card-value: added explicit `color: var(--text-card-title)` inline

### account-exists-email.html (earlier this morning)
- Removed Sign In btn + Forgot Password btn
- Updated body copy to direct users to open desktop app first
- Finalized copy: "If it was not you, don't worry, we've detected it and protected your account."
- Removed "If you didn't try to create an account..." closing line

### Key commits today
- `19dac91` await apply-pending-upgrade race condition fix
- `c5cd7c3` sessionStorage flag for reliable upgrade modal
- `64fdf84` welcome modal redesign
- `5464efb` home page margin + color fixes
- `8ae71e6` send-pending-upgrade email + edge function
# Memory — 2026-06-13 (Saturday)

## JumpKit — Major Testing & Bug Fix Session

### Pending Upgrade Flow (Pay-First → Account-Later)
- Full E2E test of LS test mode → Supabase pending_upgrades → login upgrade
- Fixed 401 on ls-webhook: redeployed with `--no-verify-jwt`
- Fixed race condition: `apply-pending-upgrade` now awaited before redirect to app.html
- Fixed modal not showing on re-test: added `sessionStorage` flag `jk_pending_upgrade_applied`
- All three cases now work cleanly:
  1. Direct to Unlimited (pay first, no account) → onboarding with Unlimited welcome screen
  2. Free signup → onboarding with Free welcome screen
  3. Free → Unlimited upgrade (existing user) → upgrade modal only

### New: send-pending-upgrade Edge Function
- Created `supabase/functions/send-pending-upgrade/index.ts`
- Created `emails/pending-upgrade-email.html`
- Fires from ls-webhook when pending_upgrades row is created (user paid, no account yet)
- Email contains: confirmation + 3-step get-started (download Mac/Win, create account, login)
- Deployed and tested — email confirmed received at feedback@jumpkit.app

### Onboarding Modal Redesign
- Step 1 is now tier-aware:
  - Free: jump-man icon, "Welcome to JumpKit Free! 🎉", Free feature list
  - Unlimited: jump-man icon, "Welcome to JumpKit Unlimited! ⚡", Unlimited feature list
- All steps pass `isUnlimited` flag through
- Step 4 (columns) button now consistently says "Save & Continue"
- Final "You're all set" screen: fixed progress bar to show 100% (was passing step 4 → 80%)
- Reverted skip-onboarding sessionStorage logic — onboarding now handles all first-login cases

### Home Page Fixes
- Margin between no-teams card and "APP FEATURES" label now consistent (20px)
- Large value text in YOUR ACCOUNT and YOUR STATISTICS cards now use `var(--text-card-title)` color

### Statistics Page Fixes
- Export PDF button hidden when click log is empty (nothing to export)
- Empty state message improved: "No launch data yet." + helpful subtext pointing to Jumps page

### Welcome to JumpKit Unlimited Modal (polish)
- Replaced comparison table with: gradient confirmation banner + checkmark feature list + gradient CTA button
- Still shows for Case 3 (existing free user upgrades later)

### account-exists-email.html (#2 email) — completed earlier
- Removed sign-in and forgot-password buttons
- Updated copy: instructions to open desktop app, sign in with existing password or reset
- Added: "If that was not you, don't worry, we've detected it and protected your account."
- Removed: "If you didn't try to create an account..." paragraph

### Test Status as of session end
- ✅ Test 1 (Direct to Unlimited): PASSED
- 🔄 Test 2 (Free signup): In progress
- ⏳ Test 3 (Free → Unlimited upgrade): Not yet run

### Key LS Test Mode Notes
- LS test mode webhook needs separate configuration from live mode
- Same checkout URL works in test mode (shows TEST MODE banner)
- Test variant IDs may differ from live — webhook logs show actual variant_id received
- Signing secret same between test/live in this setup
- LS test card: 4242 4242 4242 4242

### Supabase Reset SQL (reusable for testing)
```sql
-- Re-add pending upgrade for feedback@jumpkit.app
INSERT INTO pending_upgrades (email, tier, ls_customer_id)
VALUES ('feedback@jumpkit.app', 'core', '2256272')
ON CONFLICT (email) DO UPDATE SET tier = 'core';

-- Reset profile to free
UPDATE profiles SET subscription_tier = 'free', subscription_status = 'free'
WHERE email = 'feedback@jumpkit.app';
```

---

## Test Results — Final (2026-06-13 morning)

### All 3 flows PASSED ✅
- Test 1: Direct to Unlimited (pay first, no account) — PASSED
- Test 2: Free signup — PASSED  
- Test 3: Free → Unlimited upgrade (existing user) — PASSED

### Additional fixes applied during testing
- onboarding.js: hardcoded ROI defaults to 10s/$50 (was reading stale prefs showing 150)
- onboarding.js: split free tier team bullet into "Own 1 team (5 members, 10 shared jumps)" + "Join 1 team as a member"
- app.js: Export PDF button hidden on stats page when click log is empty
- app.js: Stats empty state message improved with helpful guidance

### Commits today (2026-06-13)
- 19dac91 await apply-pending-upgrade race condition fix
- c5cd7c3 sessionStorage flag for reliable upgrade modal
- 64fdf84 welcome modal redesign
- 5464efb home page margin + color fixes
- 8ae71e6 send-pending-upgrade email + edge function
- 8a6dc5d skip onboarding when pending upgrade (later reverted via redesign)
- c5cd7c3 sessionStorage approach
- 6f2cc6f tier-aware onboarding step 1 (Free/Unlimited)
- 052dada jump-man icon + step 4 button fix
- ad5dddb progress bar 100% on final onboarding screen
- 9f4fbf9 stats page export button + empty state
- d3734a3 team bullets split + ROI defaults hardcoded

---

## Late Morning Additions (2026-06-13)

### subscription_plan tracking
- Added `subscription_plan` column to `profiles` (monthly | annual | monthly-test | annual-test)
- Added `plan` column to `pending_upgrades`
- ls-webhook maps variant IDs: 1754948=annual, 1754951=monthly
- apply-pending-upgrade carries plan through from pending row
- Admin users page: green badge for real paid, amber badge for test users
- Account page: plan badge shown inline next to tier label
- get_admin_user_stats() RPC updated to include subscription_plan (had to DROP + recreate)
- Test users set: feedback/support = monthly-test, jeffroder@gmail.com = annual-test
- Admin users now show plan badge too (was previously gated by isUnlimitedU which excludes admins)

### Unit tests 98-103 added
- 98: apply-pending-upgrade returns applied:false (auto)
- 99: apply-pending-upgrade returns applied:true (manual)
- 100: send-pending-upgrade edge function ok:true (auto)
- 101: send-pending-upgrade inbox check (manual)
- 102: onboarding gated by onboarding_completed (auto)
- 103: upgrade modal renders correctly (auto)

### localhost email preview server
- Serve from: cd ~/Topics/JumpKit && python3 -m http.server 8099
- Email URLs: http://localhost:8099/app/emails/<name>.html

---

## JumpKit Session — 2026-06-13 Afternoon

### Home Page Team Cards — Pills Repositioned
- Pills (tier + role) moved from top of card to bottom-left, below the stats grid
- Before: `tier pill | role pill` at top, then name, then stats
- After: name → stats grid → pills row (left-justified)
- File: `app/js/app.js` in the `renderHome()` team cards section

### Teams Page — Unlimited Pill Color
- Changed "Unlimited Team" pill from purple (`#8b5cf6`) to turquoise (`#00C2C7`)
- Updated both owned team pill AND joined team pill
- File: `app/js/teams.js` lines ~205 and ~359

### Email Template Standardization (all 12 emails)
- **Outer table**: All emails now use `width="620"` fixed + `align="center"` (was `width="100%"` with `max-width:620px` on 9 of 12)
- **Margin**: All 12 now use `margin:40px auto` (team-downgrade-alert and team-lockout-warning were using `margin:20px auto`)
- **scrollbar-gutter:stable**: Added to `<head>` of all 12 emails to prevent horizontal shift when scrollbar appears on tall pages
- **Header logo**: team-downgrade-alert and team-lockout-warning updated to match reference — `display:inline-block;margin:0 8px 12px 0`, local relative src (`../../app/img/logo-dark-mode.png`)
- **Footer logo**: Same two emails updated to use relative src
- **Head tag**: Inline comments removed from head tag in team-downgrade and team-lockout emails

### Team Lockout / Downgrade Feature — 7 Deliverables Built Earlier
(Details in earlier session entry above)
---

## JumpKit Session — 2026-06-13 Morning

### Pending Upgrade Flow (Pay Before Account) — FULLY TESTED ✅
- `pending_upgrades` table stores tier + ls_customer_id when webhook fires with no matching profile
- `ls-webhook` redeployed with `--no-verify-jwt` (was returning 401 to LS)
- `apply-pending-upgrade` edge function: applies tier on login, deletes row, fires send-welcome-core
- **Bug fixed:** was fire-and-forget; changed to `await` in auth.js so upgrade applies before redirect to app.html
- **Bug fixed:** modal not showing on re-test — used `sessionStorage.setItem('jk_pending_upgrade_applied', '1')` in auth.js, checked in app.js to reliably show modal regardless of `last_known_tier`

### New: send-pending-upgrade Edge Function ✅
- Fires from `ls-webhook` in the 0-row case (user paid, no account yet)
- Sends branded onboarding email: "Your JumpKit Unlimited subscription is confirmed ⚡"
- 3-step getting started: Download (Mac/Win links) → Create account with same email → Login (auto-applies)
- Email template: `app/emails/pending-upgrade-email.html`
- Edge function: `app/supabase/functions/send-pending-upgrade/index.ts`
- Deployed with `--no-verify-jwt`
- Tested and confirmed ✅

### Welcome to JumpKit Unlimited Modal Redesign
- Replaced comparison grid (Free vs Unlimited) with:
  - Lightning bolt icon in modal title (teal)
  - Gradient teal/blue confirmation banner with checkmark circle
  - Feature checklist with gradient circle checkmarks
  - Full-width gradient "Let's Go →" CTA button

### Home Page Fixes
- No-teams card: added `margin-bottom: 20px` to match `.home-teams-grid` spacing
- Account/stats stat-card-value: added explicit `color: var(--text-card-title)` inline

### account-exists-email.html (earlier this morning)
- Removed Sign In btn + Forgot Password btn
- Updated body copy to direct users to open desktop app first
- Finalized copy: "If it was not you, don't worry, we've detected it and protected your account."
- Removed "If you didn't try to create an account..." closing line

### Key commits today
- `19dac91` await apply-pending-upgrade race condition fix
- `c5cd7c3` sessionStorage flag for reliable upgrade modal
- `64fdf84` welcome modal redesign
- `5464efb` home page margin + color fixes
- `8ae71e6` send-pending-upgrade email + edge function
# Memory — 2026-06-13 (Saturday)

## JumpKit — Major Testing & Bug Fix Session

### Pending Upgrade Flow (Pay-First → Account-Later)
- Full E2E test of LS test mode → Supabase pending_upgrades → login upgrade
- Fixed 401 on ls-webhook: redeployed with `--no-verify-jwt`
- Fixed race condition: `apply-pending-upgrade` now awaited before redirect to app.html
- Fixed modal not showing on re-test: added `sessionStorage` flag `jk_pending_upgrade_applied`
- All three cases now work cleanly:
  1. Direct to Unlimited (pay first, no account) → onboarding with Unlimited welcome screen
  2. Free signup → onboarding with Free welcome screen
  3. Free → Unlimited upgrade (existing user) → upgrade modal only

### New: send-pending-upgrade Edge Function
- Created `supabase/functions/send-pending-upgrade/index.ts`
- Created `emails/pending-upgrade-email.html`
- Fires from ls-webhook when pending_upgrades row is created (user paid, no account yet)
- Email contains: confirmation + 3-step get-started (download Mac/Win, create account, login)
- Deployed and tested — email confirmed received at feedback@jumpkit.app

### Onboarding Modal Redesign
- Step 1 is now tier-aware:
  - Free: jump-man icon, "Welcome to JumpKit Free! 🎉", Free feature list
  - Unlimited: jump-man icon, "Welcome to JumpKit Unlimited! ⚡", Unlimited feature list
- All steps pass `isUnlimited` flag through
- Step 4 (columns) button now consistently says "Save & Continue"
- Final "You're all set" screen: fixed progress bar to show 100% (was passing step 4 → 80%)
- Reverted skip-onboarding sessionStorage logic — onboarding now handles all first-login cases

### Home Page Fixes
- Margin between no-teams card and "APP FEATURES" label now consistent (20px)
- Large value text in YOUR ACCOUNT and YOUR STATISTICS cards now use `var(--text-card-title)` color

### Statistics Page Fixes
- Export PDF button hidden when click log is empty (nothing to export)
- Empty state message improved: "No launch data yet." + helpful subtext pointing to Jumps page

### Welcome to JumpKit Unlimited Modal (polish)
- Replaced comparison table with: gradient confirmation banner + checkmark feature list + gradient CTA button
- Still shows for Case 3 (existing free user upgrades later)

### account-exists-email.html (#2 email) — completed earlier
- Removed sign-in and forgot-password buttons
- Updated copy: instructions to open desktop app, sign in with existing password or reset
- Added: "If that was not you, don't worry, we've detected it and protected your account."
- Removed: "If you didn't try to create an account..." paragraph

### Test Status as of session end
- ✅ Test 1 (Direct to Unlimited): PASSED
- 🔄 Test 2 (Free signup): In progress
- ⏳ Test 3 (Free → Unlimited upgrade): Not yet run

### Key LS Test Mode Notes
- LS test mode webhook needs separate configuration from live mode
- Same checkout URL works in test mode (shows TEST MODE banner)
- Test variant IDs may differ from live — webhook logs show actual variant_id received
- Signing secret same between test/live in this setup
- LS test card: 4242 4242 4242 4242

### Supabase Reset SQL (reusable for testing)
```sql
-- Re-add pending upgrade for feedback@jumpkit.app
INSERT INTO pending_upgrades (email, tier, ls_customer_id)
VALUES ('feedback@jumpkit.app', 'core', '2256272')
ON CONFLICT (email) DO UPDATE SET tier = 'core';

-- Reset profile to free
UPDATE profiles SET subscription_tier = 'free', subscription_status = 'free'
WHERE email = 'feedback@jumpkit.app';
```

---

## Test Results — Final (2026-06-13 morning)

### All 3 flows PASSED ✅
- Test 1: Direct to Unlimited (pay first, no account) — PASSED
- Test 2: Free signup — PASSED  
- Test 3: Free → Unlimited upgrade (existing user) — PASSED

### Additional fixes applied during testing
- onboarding.js: hardcoded ROI defaults to 10s/$50 (was reading stale prefs showing 150)
- onboarding.js: split free tier team bullet into "Own 1 team (5 members, 10 shared jumps)" + "Join 1 team as a member"
- app.js: Export PDF button hidden on stats page when click log is empty
- app.js: Stats empty state message improved with helpful guidance

### Commits today (2026-06-13)
- 19dac91 await apply-pending-upgrade race condition fix
- c5cd7c3 sessionStorage flag for reliable upgrade modal
- 64fdf84 welcome modal redesign
- 5464efb home page margin + color fixes
- 8ae71e6 send-pending-upgrade email + edge function
- 8a6dc5d skip onboarding when pending upgrade (later reverted via redesign)
- c5cd7c3 sessionStorage approach
- 6f2cc6f tier-aware onboarding step 1 (Free/Unlimited)
- 052dada jump-man icon + step 4 button fix
- ad5dddb progress bar 100% on final onboarding screen
- 9f4fbf9 stats page export button + empty state
- d3734a3 team bullets split + ROI defaults hardcoded

---

## Late Morning Additions (2026-06-13)

### subscription_plan tracking
- Added `subscription_plan` column to `profiles` (monthly | annual | monthly-test | annual-test)
- Added `plan` column to `pending_upgrades`
- ls-webhook maps variant IDs: 1754948=annual, 1754951=monthly
- apply-pending-upgrade carries plan through from pending row
- Admin users page: green badge for real paid, amber badge for test users
- Account page: plan badge shown inline next to tier label
- get_admin_user_stats() RPC updated to include subscription_plan (had to DROP + recreate)
- Test users set: feedback/support = monthly-test, jeffroder@gmail.com = annual-test
- Admin users now show plan badge too (was previously gated by isUnlimitedU which excludes admins)

### Unit tests 98-103 added
- 98: apply-pending-upgrade returns applied:false (auto)
- 99: apply-pending-upgrade returns applied:true (manual)
- 100: send-pending-upgrade edge function ok:true (auto)
- 101: send-pending-upgrade inbox check (manual)
- 102: onboarding gated by onboarding_completed (auto)
- 103: upgrade modal renders correctly (auto)

### localhost email preview server
- Serve from: cd ~/Topics/JumpKit && python3 -m http.server 8099
- Email URLs: http://localhost:8099/app/emails/<name>.html

---

## Afternoon Session (2026-06-13)

### Team Member Lockout System — 7 Deliverables Built
1. **DB migration** — added `locked`, `lock_at`, `lock_notified_2day` columns to `team_members`
2. **team-downgrade-alert-email.html** — immediate notice to owner when subscription cancels; lists over-cap members with lock date + re-upgrade CTA
3. **team-lockout-warning-email.html** — 2-day warning email; owner variant (lists members) + member variant (tells them to contact owner)
4. **Edge Function: send-team-downgrade-alert** — fires immediately on cancel; sends alert to team owner
5. **Edge Function: check-member-lockouts** — daily scheduled job; sends 2-day warnings + locks members when lock_at passes
6. **ls-webhook updated** — marks over-cap members on cancel (sets lock_at = now+14 days), clears all locks on re-upgrade
7. **Pills + locked-member UI** — home page team cards + teams page

### UI Polish
- Home page team cards: pills (tier + role) moved to bottom of card, left-justified, below stats grid
- Teams page: "Unlimited Team" pill changed from purple (#8b5cf6) to turquoise (#00C2C7)
- Admin users table: "Subscription" col renamed to "TIER"; new "SUBSCRIPTION" col added to the right showing monthly/annual pills (amber = monthly/test, green = annual)

### Email Standardization (all 12 emails)
- All emails: `width="620"` fixed table, `align="center"`, `scrollbar-gutter:stable`, `margin:40px auto`
- team-downgrade-alert + team-lockout-warning: headers now identical to other emails (local relative img paths)
- Both new emails: sample member list added for preview (real data injected by edge function at send time)
- jumpkit_docs.html changelog updated with today's work; changelog titles fixed to date-only format

### New Favicon — Complete Icon Refresh
- Source: `app/assets/jump-icon.svg` (running man, transparent bg)
- Design: black running man on dark navy → royal blue → turquoise gradient, rounded corners
- Generated all sizes in `app/assets/final/`:
  - `icon.icns` — macOS app icon
  - `icon.ico` — Windows app icon
  - PNG sizes: 16, 24, 32, 48, 64, 128, 256, 512, 1024
  - Landing favicons: favicon-32.png, favicon.svg, apple-touch-icon.png, icon-192.png, icon-512.png
- Deployed to macOS (`assets/icon.icns`), Windows (`assets/icon.ico`), and landing page (`landing/`)
- Committed and pushed to Vercel — live at jumpkit.app
- Safari private window (Cmd+Shift+N) required to bypass favicon cache
