# Memory — 2026-05-20

## JumpKit UI Session

### Completed Today

- ✅ Save Settings button → changed from `btn-subtle` to `btn-save` (green tint, matches Add Jump modal)
- ✅ Sidebar CTA height reduced 15% (`padding: 9.3px → 7.9px`)
- ✅ Gradient unified: `--grad` updated to `linear-gradient(135deg, #50CACC, #1A4FD6)` in vars.css — affects all `btn-primary` buttons + user avatar
- ✅ Auth submit buttons (login, signup, join) also get same gradient override in auth.css
- ✅ Sign In button: `ti-login` icon added; Create Account: `ti-user-plus` icon added
- ✅ Icons: `inline-flex`, `align-items:center`, `gap:6px`, `white-space:nowrap`, `flex-shrink:0`; sized `1.32rem`
- ✅ Sign In click → icon swaps to spinning `ti-loader-2`, button disabled; restores on error
- ✅ Free-tier column cap flash fix:
  - Root cause: `_ownedTeamIds` defaulted to `[]` before async load → Jeff's team ID not found → `isReceivedShared=true` → paywall cap fired on first render
  - Fix: `window._ownedTeamIds` no longer defaults to `[]`; cap only fires when `Array.isArray(_ownedTeamIds)` is confirmed
  - Also changed `_freeTier` to only use `window._supabaseProfile?.subscription_tier` (no localStorage fallback) to prevent stale 'free' flicker
  - Cap now applies to ALL shared columns for free tier (not just received ones) — consistent with save-time enforcement
- ✅ Settings page auto-archive dropdown overflow fix: `.acct-section:has(.custom-select) { overflow: visible }` — targeted, doesn't affect other sections
- ✅ Stats page Top Jumps scrollbar: `margin-right:-18px; padding-right:18px` — scrollbar now hugs card right edge
- ✅ Version bumped: `jumps.js?v=1779311909`
- ✅ Backup: `app_backup_2026-05-20`
- ✅ `TODO.md` created at `Topics/JumpKit/TODO.md`

### Files Changed Today
- `app/css/vars.css` — `--grad` updated
- `app/css/app.css` — sidebar CTA padding, acct-section overflow fix
- `app/css/auth.css` — gradient override for auth submit buttons
- `app/js/app.js` — Save Settings btn class, stats Top Jumps scrollbar
- `app/js/jumps.js` — free-tier cap flash fix (`_freeTier`, `isReceivedShared`)
- `app/js/auth.js` — Sign In icon swap + spinner on click
- `app/index.html` — Sign In / Create Account button icons
- `app/app.html` — jumps.js version bump

### Plan for Tomorrow
- Teams page UI cleanup — tighten My Account / My Teams unified layout
