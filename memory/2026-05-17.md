# Memory — 2026-05-17

## Completed Today

### Email Templates
- ✅ `send-welcome` Edge Function — fires on first sign-in (guarded by `profiles.welcome_email_sent`); `await fetch()` before redirect; deployed
- ✅ `send-welcome-core` Edge Function — triggers from `ls-webhook` on `subscription_created`
- ✅ `send-cancellation` Edge Function — triggers from `ls-webhook` on `subscription_cancelled`/`subscription_expired`
- ✅ `icon-jumpkit-white.png` generated + deployed to `https://jumpkit.app/email-icons/`
- ✅ All email headers consistent: `display:block;margin:0 auto 12px` on logo
- ✅ Supabase Auth templates (confirm + reset) re-pasted with corrected logo styles
- ✅ `ls-webhook` updated to call `send-welcome-core` and `send-cancellation` on relevant events; redeployed

### Supabase DB Changes
- ✅ `profiles.welcome_email_sent boolean DEFAULT false` — added via SQL Editor
- ✅ `profiles.onboarding_completed boolean DEFAULT false` — added via SQL Editor

### 3-Step Onboarding Modal (`app/js/onboarding.js`)
- ✅ Step 1: Welcome screen (jumping man SVG icon, "Let's Go" button)
- ✅ Step 2: Configure Columns (name inputs + visible checkboxes, saves via `DB.saveColumns()`)
- ✅ Step 3: Add First Jump (name, URL, custom-select column dropdown with fixed positioning)
- ✅ Spinner on Finish Setup (1.4s rotation), then Setup Complete screen
- ✅ "Go to Your Jumps Page" button calls `navigateTo('jumps')`
- ✅ Progress bar at top of modal (33% → 67% → 100%)
- ✅ Tracked server-side via `profiles.onboarding_completed`
- ✅ Loaded via `<script src="js/onboarding.js">` in `app.html`

### Auth/Login
- ✅ Welcome email fires on first sign-in (not signup); `auth.js` calls `send-welcome` with `userId`, `email`, `firstName`

### Docs & Backup
- ✅ `JUMPKIT_DOCS.html` — Section 9 (Onboarding), 2026-05-17 changelog, last updated date
- ✅ `app_backup_2026-05-17` — morning backup
- ✅ `app_backup_2026-05-17b` — afternoon backup

### Git Fix
- ✅ `node_modules/` added to `.gitignore`; untracked via `git rm -r --cached`
- ⚠️ Git repo had corrupt object error after large removal commit — push to GitHub may be broken; needs `git fsck` repair

### Vercel
- ✅ Landing redeployed after 404 on `/confirmed` (caused by git corruption blocking previous deploy)

## Open TODOs
- [ ] Fix git repo corrupt object (`fatal: unable to read be1cf517...`) — run `git fsck` and repair
- [ ] Deploy `send-welcome-core` and `send-cancellation` Edge Functions (written but not yet deployed)
- [ ] Deploy updated `ls-webhook` to Supabase
- [ ] Welcome to JumpKit Core — test with a real paid subscription event
- [ ] Cancellation email — test
- [ ] Complete Teams E2E Tests 4.1 (remove member) and 4.2 (delete team)
- [ ] Move `send-feedback.js` → `supabase/functions/send-feedback/index.ts`
- [ ] Wire "Get JumpKit Free Tier" button → Lemon Squeezy
- [ ] Dev/prod Supabase separation
- [ ] Rebuild Windows installer
- [ ] Pagination on list queries

## Key Identifiers
- New Edge Functions: `send-welcome` (deployed ✅), `send-welcome-core` (not yet deployed), `send-cancellation` (not yet deployed)
- Onboarding file: `Topics/JumpKit/app/js/onboarding.js`
- Git corruption: object `be1cf517c9cde53a9290fbe61a8472f56a4e8eb3`
- Backups: `app_backup_2026-05-17`, `app_backup_2026-05-17b`
