# 2026-06-17 — JumpKit Session Log

## Export / Import overhaul
- Export now filters to personal-only jumps+cols (no shared, no team, no empty cols); archived personal jumps included
- Import shows col-by-col selection modal; archived jumps treated as separate checkbox group; dedup skips same URL in same col
- Import Complete modal lists skipped jumps by name + reason (Duplicate / Unselected collection)
- Import intro modal added before file picker with instructions
- After import, `navigateTo('jumps')` called so sidebar nav stays in sync
- Export filename format changed: `jumpkit-backup-2026-06-17_3-05pm.json`
- Export success modal: bold main message + muted helper text

## Configure Columns modal — Add / Remove
- Add Column button appends blank row; new rows default to Personal badge in Sharing column
- Remove (trash) button: disabled for shared/team cols with tooltip; personal cols with jumps show inline confirm
- Inline confirm spans full row width (display:block); uses `btn-delete` class (dark-mode safe)
- "Yes, Delete" immediately removes col+jumps from DB and calls `renderColumns()` — no need to Save first
- Trash icon uses `color:inherit` + `data-tooltip` (matches teams page style)

## Pure logic module + tests
- `js/logic.js` created: zero-dep module with buildExportData, partitionBackupJumps, isDuplicateJump, removedPersonalColIds, orphanedJumps, colPadCount
- `app.js` and `jumps.js` now delegate to JK.logic (no inline duplication — tests prove real code paths)
- 23 Node unit tests in `test/logic.test.mjs` (npm test); all passing
- 6 new tests added to tests page: IDs 156-161 (exec #127-132); Account + Columns sections; 156-160 auto, 161 manual
- `logic.js` was missing from `app.html` (actual Electron shell) — fixed
- Section expand max-height bumped 6000px → 60000px (was clipping after exec #82)

## Column limit policy
- No col limit on free or unlimited tier (paywall is 250 launch cap, not col count)
- Default 10 empty slots on first open; Add Column allows going beyond freely
- Team cols: hide-only, no remove

## Commits today (chronological)
- export/import personal-only + col-selection modal
- configure-cols add/remove buttons
- logic.js + 23 unit tests
- tests page IDs 156-161
- app.html logic.js fix
- section expand height fix
- app.js/jumps.js delegate to JK.logic
- export filename format
- export modal text polish
- import intro modal
- import complete skipped jump list
- delete confirm layout + btn-delete style
- Yes Delete immediate DB removal + renderColumns
- trash btn matches teams page (color:inherit, data-tooltip)
- new col rows show Personal badge
- navigateTo('jumps') after import

## Test 155 result
- Jeff manually verified export/import round-trip: ✅ working
# 2026-06-17 — JumpKit Session Log

## Export / Import overhaul
- Export now filters to personal-only jumps+cols (no shared, no team, no empty cols); archived personal jumps included
- Import shows col-by-col selection modal; archived jumps treated as separate checkbox group; dedup skips same URL in same col
- Import Complete modal lists skipped jumps by name + reason (Duplicate / Unselected collection)
- Import intro modal added before file picker with instructions
- After import, `navigateTo('jumps')` called so sidebar nav stays in sync
- Export filename format changed: `jumpkit-backup-2026-06-17_3-05pm.json`
- Export success modal: bold main message + muted helper text
- Export intro modal added (matching import intro style) — shown before file save dialog
- Invalid file errors now show proper modal (not just toast): non-JSON and valid-JSON-but-not-JumpKit both handled

## Configure Columns modal — Add / Remove
- Add Column button appends blank row; new rows default to Personal badge in Sharing column
- Remove (trash) button: disabled for shared/team cols with tooltip; personal cols with jumps show inline confirm
- Inline confirm spans full row width (display:block); uses `btn-delete` class (dark-mode safe)
- "Yes, Delete" immediately removes col+jumps from DB and calls `renderColumns()` — no need to Save first
- Trash icon uses `color:inherit` + `data-tooltip` (matches teams page style)

## Pure logic module + tests
- `js/logic.js` created: zero-dep module with buildExportData, partitionBackupJumps, isDuplicateJump, removedPersonalColIds, orphanedJumps, colPadCount
- `app.js` and `jumps.js` now delegate to JK.logic (no inline duplication — tests prove real code paths)
- 23 Node unit tests in `test/logic.test.mjs` (npm test); all passing
- 6 new tests added to tests page: IDs 156-161 (exec #127-132); Account + Columns sections; 156-160 auto, 161 manual
- `logic.js` was missing from `app.html` (actual Electron shell) — fixed
- Section expand max-height bumped 6000px → 60000px (was clipping after exec #82)
- Test 131 updated: code blocks with copy buttons, numbered steps, simplified text

## Column limit policy
- No col limit on free or unlimited tier (paywall is 250 launch cap, not col count)
- Default 10 empty slots on first open; Add Column allows going beyond freely
- Team cols: hide-only, no remove

## Select Collections modal polish
- Last row border-bottom removed
- Import Selected btn icon: color:inherit
- Import intro modal Choose File btn icon: color:inherit

## Commits today (chronological)
- export/import personal-only + col-selection modal
- configure-cols add/remove buttons
- logic.js + 23 unit tests
- tests page IDs 156-161
- app.html logic.js fix
- section expand height fix
- app.js/jumps.js delegate to JK.logic
- export filename format
- export modal text polish
- import intro modal
- import complete skipped jump list
- delete confirm layout + btn-delete style
- Yes Delete immediate DB removal + renderColumns
- trash btn matches teams page (color:inherit, data-tooltip)
- new col rows show Personal badge
- navigateTo('jumps') after import
- export intro modal
- invalid import file error modals
- test 131 code block + numbered steps

## Test results
- Test 155 (export/import round-trip): ✅ manually verified by Jeff
