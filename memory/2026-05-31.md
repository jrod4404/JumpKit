# 2026-05-31

## JumpKit — Paywall Testing + UX Polish Session

Long session working through the to-do list, primarily #1 (paywall edge cases).

### Infrastructure Fix
- **Supabase SMTP sender** updated from `noreply@jumpkit.ai` → `noreply@jumpkit.app` — was causing 500 errors on new account signup confirmation emails

### Teams Page Fixes
- `renderTeams()` now targets `acctTabContent` when present — prevents tab bar (My Account / My Teams / Settings) from being wiped on team create/edit/delete actions
- "No teams yet." empty state updated to "No teams yet. Click **Create Team** to get started." (3 locations in teams.js)
- "None yet — click Share Col above" → "None yet — click **Manage Sharing** above to add one"

### Account Page Subtitles (per-tab)
- My Account tab → "View your account details"
- My Teams tab → "Manage your teams and shared jumps"
- Settings tab → "Manage settings to personalize JumpKit behavior"
- Subtitles set dynamically on tab switch via `setAcctSubtitle()`

### Account Type Labels
- Free → "JumpKit Free"
- Core → "JumpKit Core"
- Jet AI → "JumpKit + Jet AI" (unchanged)

### Features Button
- Added inline "Features" button next to Account Type on My Account tab
- Opens `openTierFeaturesModal()` — tier-specific feature list with checkmarks
- Free modal includes upgrade nudge + "Unlock JumpKit Core" button
- Core modal shows all 6 features, Close button only

### Free Tier Paywall Rules Enforced
- **Own max 1 team** — already existed in `openAddTeamModal`
- **Join max 1 team** — NEW: `openJoinTeamModal` now checks `team_members` count, blocks with upgrade modal if ≥ 1
- Features modal updated: "Own 1 team" + "Join 1 team as a member"
- Core features: "Own unlimited teams" + "Join unlimited teams as a member"

### Upgrade Banner (Account page, free users)
- Updated feature list to match new rules (6 items)
- Switched from `<ul>` bullets to turquoise checkmarks in 2-column grid

### Welcome to JumpKit Core Modal (NEW)
- Fires on first login after free → core upgrade (detected via `last_known_tier` on profiles)
- Shows before/after comparison table: Free feature → turquoise arrow → Core feature
- 6 comparison rows with no borders
- "Free" / "Core" column headers aligned above respective columns
- OK button (btn-subtle, no gradient)
- JumpKit running man icon in modal header
- Turquoise checkmark icon (40px) + subtitle text on same line

### Subscription Ended Modal Fixes
- 250 launches bullet now appears **first** (unshift instead of push)
- Team name lookup falls back to direct Supabase query for members who don't own the team — fixes "your team" placeholder showing instead of actual team name

### Invite Email Updates
- Bullet 1: "Download and open JumpKit" → "Download the JumpKit desktop app"
- Deployed to Supabase: `send-invite` edge function redeployed

### Invite Members Modal
- Helper text added above Team Password field
- Join Team modal: added "You can find this in your invitation email." to password prompt

### Signup Success Modal
- Text updated to include "Then go to the JumpKit sign in page to sign in."
- "Go to Sign In" button now has white left-arrow icon

### Docs Updated
- Section 1.4 Pricing Tiers — renamed Free to "JumpKit Free", added team limits to both tiers
- Paywall enforcement rules documented in code comment block
- Changelog 2026-05-31 added

### Backups
- `app_backups/app_backup_2026-05-31` (mid-session)
- `app_backups/app_backup_2026-05-31b` (end of session)

### Verified E2E Today
- Create team → invite member → member creates account → onboarding → join team → shared column visible ✅
- Upgrade free → core → Welcome modal fires ✅
- Downgrade core → free → Subscription Ended modal fires ✅
- Feedback email: app → Resend → Proton Mail ✅
