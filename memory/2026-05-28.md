# Memory — 2026-05-28

## JumpKit Security — Item #11 Complete ✅

### Event Delegation Refactor (Item #11)

Completed the full event delegation refactor across all JS files. Previously started on `jumps.js` in the 2026-05-27 session (backup: `app_backup_2026-05-27b`). Jeff confirmed "no issues seen" after reload.

Today (2026-05-28) completed remaining files:

**Files refactored:**
- `js/jumps.js` ← done 2026-05-27
- `js/archive.js` ← done today (9 handlers, `data-jaction` prefix: `arc-*`)
- `js/sync.js` ← done today (4 handlers: `stale-keep`, `stale-remove`, `stale-upgrade`)
- `js/app.js` ← done today (19 handlers: `notif-mark-read`, `open-feedback-modal`, `force-backup`, `save-account-prefs`, `submit-feedback`, `open-url`, `open-url-close`, `nav-teams-close`, etc.)
- `js/teams.js` ← done today (61 handlers: `t-*` prefix, also `open-add-team-modal`)
- `js/auth.js` ← done today (2 anchor onclick → `data-jaction="show-view"`)
- `index.html` ← done today (2 anchor onclick → `data-jaction="show-view"`)

**Key pattern:** All inline `onclick=` replaced with `data-jaction="..."` + `data-*` params.
Each file has a `document.addEventListener('click', ...)` delegation handler at the bottom.
`data-stop-prop="1"` used for buttons inside clickable rows (teams panel) to call `e.stopPropagation()`.

**QuerySelector fixes:**
- `[onclick="saveAddTeam()"]` → `[data-jaction="t-save-add-team"]`
- `[onclick="doChangeTeamPassword(...)"]` → `[data-jaction="t-do-change-pw"]`

**CSP updated in main.js:**
- Removed `'unsafe-inline'` from `script-src` — fully locked down

**Backup:** `app_backups/app_backup_2026-05-28`

---

## Session Continued — Afternoon

### UI Fixes (feedback modal + misc)
- ✅ Feedback modal "Thanks" screen: circle-check icon → green (`#22c55e`), centered (`margin:0 auto`)
- ✅ Feedback modal "Your Name" field: pre-fills `first_name + ' ' + last_name` from `window._supabaseProfile`, fallback to `currentUser.name`
- ✅ Feedback modal Submit button: `btn-subtle` → `btn-save` (green, matches Add Jump)

### One-to-Many Column Sharing Refactor ✅

**Problem:** Column-to-team sharing was one-to-one. A column could only be shared with one team at a time. Second team's toggle was grayed out and disabled.

**Solution:** Introduced `sharedTeams: [{ teamId, supabaseId }]` on columns and `supabaseIdMap: { [teamId]: supabaseId }` on jumps. Old `teamId`/`supabaseId` format preserved for backward compat.

**Files changed:**
- `js/jumps.js` — `syncColumnToSupabase(col, teamId)` new signature; `unshareColumnFromSupabase(col, teamId)` new signature; `renderColConfigModal` shows all team badges; `saveColumns` rename sync handles sharedTeams; `openConfigColumnsModal` collects teamIds from sharedTeams
- `js/teams.js` — Share modal: removed disabled/opacity logic, all columns toggleable per-team; `_applyShareColumnSave` migrates old format and updates sharedTeams; team delete strips only deleted team from sharedTeams
- `js/sync.js` — Stale detection skips new-format (sharedTeams) columns; column upsert matches and updates sharedTeams entries

**Backup:** `app_backups/app_backup_2026-05-28b`

---

### Security Items Completed This Session
- ✅ #13 — Hardcoded anon key: removed from `app.js` + `auth.js`, both now use `window.SUPABASE_ANON_KEY` / `window.SUPABASE_URL`
- ✅ #14 — Hardcoded admin email: replaced with `role === 'admin'` check; Supabase constraint updated to allow `'admin'`; `schema.sql` updated
- ✅ #15 — `isShared` 1/0 vs boolean: 15 occurrences fixed across `sync.js`, `teams.js`, `jumps.js` → all now use `true`/`false`
- ✅ Modal bug fix: confirm-unshare modal now properly replaces manage-sharing modal (`Modal.close()` added before re-open)

- ✅ #10 — `tests.js` (130KB): removed from `app.html`, lazy-loaded in `navigateTo('tests')` for admin only
- ✅ #12 — `chart.min.js` (200KB): removed from `app.html`, lazy-loaded in `renderStats()` via `loadScript()` helper
- ✅ Bonus: hardcoded email in `tests.js` `renderTests()` also fixed → `role === 'admin'`
- ✅ Backup: `app_backup_2026-05-28c`

### Remaining Medium Items (#10–#19 minus #10, #11, #12, #13, #14, #15)

| # | Issue | Status |
|---|-------|--------|
| 10 | tests.js (130KB) ships to all users | To do |
| 12 | chart.min.js (200KB) loaded on every page | To do |
| 13 | auth.js hardcodes anon key JWT literal | To do |
| 14 | Admin check uses hardcoded email | To do |
| 15 | isShared mixed 1/0 vs boolean in sync layer | To do |
| 16 | Stale data cleanup two ID schemes → ghost rows | To do |
| 17 | Dead code in main.js (globalShortcut, tray) | To do |
| 18 | Auto-seed triple-guard race | To do |
| 19 | getColumns sort inconsistency IPC vs cache | To do |

Low (#20, #21) — decided to skip or batch with housekeeping.
