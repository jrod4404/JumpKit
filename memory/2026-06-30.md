# Memory — 2026-06-30

## JumpKit Session (morning/afternoon)

### Bug Fix: Jumps missing after login
- Root cause: Old v1.0.0 (localStorage-only) was installed. New builds use SQLite + Supabase auth. The old app read from now-empty localStorage.
- Fix: Installed new v0.1.0 Prod arm64 build from dist folder. Data (41 active jumps, 4 columns, Core tier) was intact in SQLite.

### tests.js — Manage Testing Modal changes
- `ti-clipboard-check` was missing from tabler sprite — replaced with `ti-clipboard-list`
- Icon swap: "Active testing session" card → `ti-clipboard-list`; "Testing complete!" card → `ti-file-check`
- Both icons set to 2.3rem, color #3fbe71, matching style
- "Go to Deployments" button: matched width (172px fixed), font 0.92rem, icon 0.98rem — same as Clear Session button
- "Testing complete!" alert: matched full width to "Active testing session" card (no flex:1 sizing mismatch)

### deployment.js — Deployment Checklist changes
- **Redundancy analysis done** (no changes made to the checks themselves yet — Jeff to decide)
  - Major: cv-6 duplicates lp-2 + lp-3 (all update landing page URLs/version)
  - Minor: cv-7 and lp-4 overlap (both commit+push)
  - Minor: rel-2a + rel-2b can merge into one GitHub release verify step
- **Automation buttons added** ("Auto Check") to 5 steps:
  - cv-2: fetches latest commit ID via existing `getLatestCommitId` IPC
  - bl-7: new `listDistFiles` IPC → shows installer filenames + sizes, warns if < 20 MB
  - lp-4a: fetch to https://www.jumpkit.app + http redirect check
  - rel-2a: GitHub API fetch → shows tag_name + publish date
  - rel-2b: GitHub API fetch → checks for latest-mac.yml + latest.yml assets
- Auto Check button: `ti-player-play` icon, `btn btn-subtle` class, same size/style as To Do button
- **Run Check column**: dedicated `<td>` between step text and To Do column (110px wide)
- **To Do / Done button → opens step detail modal**:
  - Shows step number, phase, status badge
  - Full step details HTML rendered
  - Command block with copy button (if step has cmd)
  - Notes textarea (auto-saves to `jk_deploy_notes` in localStorage)
  - Footer: Mark as Done (green) · Mark as To Do (grey) · Close
  - On action: saves notes, updates state, closes modal, re-renders with sections preserved

### main.js + preload.js
- Added `listDistFiles` IPC handler and preload entry to support bl-7 auto check

## Status at end of session
- JumpKit app running and healthy on Jeff's Mac mini
- All changes in source files, not yet built/packaged into a new release
