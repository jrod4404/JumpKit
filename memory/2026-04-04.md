# 2026-04-04 — Session Notes

## JumpKit Progress

### Feedback Form — Fully Wired ✅
- Created `supabase/send-feedback.js` Edge Function (Resend-based)
- Wired `submitFeedback()` in `app.js` to call it with correct JWT anon key
- Fixed 401 (wrong anon key format) and 500 (domain not verified) errors
- Temporarily sending to `jeffroder@gmail.com` from `onboarding@resend.dev`
- TODO: Once `jumpkit.ai` verified in Resend → swap `from` to `noreply@jumpkit.ai` and `to` to `support@jumpkit.ai`, redeploy edge function

### Supabase Anon Key (JWT)
- Updated in `app.js` and `supabase/config.js`
- Key starts with `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`

### UI Polish
- Modal overlay click-to-close disabled — must use buttons
- Add Jump + Save Columns save buttons changed to `btn-subtle` (no green)
- 1-second spinner on both save buttons before close + success/fail toast
- Column dropdown: active border matches form-input (hover border removed)
- Column dropdown selected text color fixed (clears inline dim style)
- Spin CSS class added to `app.css`
- Archive empty state updated to `placeholder-page` style (matches Jet AI)
- `openFeedbackModal` and `submitFeedback` exposed on `window`
- Submit feedback button changed to `btn-subtle`

### Unit Tests
- Added Tests 46–51 (T1–T6): Team Sharing suite hitting real Supabase DB
  - T1: Create test team
  - T2: Share column to team
  - T3: Invite user (pending status)
  - T4: Accept invitation
  - T5: Remove member
  - T6: Unshare column + full cleanup
- Tests 43–45 (Settings) reordered to correct sequential position
- Pass/Fail/Manual result badges styled as pill buttons

## Pending
- ✅ Feedback email fully live — `noreply@jumpkit.ai` → `support@jumpkit.ai`, domain verified, edge function deployed
- Supabase schema access for unit test queries (need schema file or direct access)
- Team sharing evaluation (tests 46–51) to be run and validated
- **Manually verify unit tests 46–51 step by step:**
  - T1 (46): Create a real test team in the Teams page, then run test to confirm Supabase row exists
  - T2 (47): Share a column to that team via Configure Columns, then run test to confirm shared_columns + shared_jumps rows
  - T3 (48): Invite a user to the team via Teams page, then run test to confirm team_invites status = pending
  - T4 (49): Accept the invitation via the app (second user), then run test to confirm status = accepted
  - T5 (50): Remove the user from the team via Teams page, then run test to confirm team_members row deleted
  - T6 (51): Unshare the column via Configure Columns, then run test to confirm shared_columns + shared_jumps rows removed
