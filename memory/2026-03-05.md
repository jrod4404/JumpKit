# Memory — March 5, 2026

## JumpKit Dev Session (Thu Mar 5)

### Stats Page — Fixed & Fully Built
- Stats page was blank due to Chart.js CDN blocking local scripts — fixed by bundling chart.js locally (`js/chart.min.js`)
- Stats fully inline in `app.js` (not stats.js) — stats.js is now essentially unused
- Filter slider uses `.jump-filter-bar` / `.jfb-tab` / `.jfb-slider` classes (same as jumps page)
- **View data ranges (final):**
  - Summary: all-time
  - Daily: this week (one bar per day, Sun–Sat)
  - Weekly: last 52 calendar weeks (one bar per week, labels every 4th)
  - Monthly: this year Jan–Dec (one bar per month)
  - Yearly: last 4 full years + current year YTD (5 bars)

### Settings / Account Page Split
- **Settings** (nav item, below Statistics): all preference toggles — starting page, notifications, cloud backup, time/money saved, show description, show hotkey, auto-archive
- **My Account** (user dropdown): profile only — name, email, account type, payment status, org, team, role, member since + Send Feedback button
- Feedback modal removed from user dropdown, now lives on My Account page

### Bug Fixes
- Hotkey recorder: removed `readonly` attribute, added click-to-activate recording mode with turquoise border + blur-to-deactivate
- Starting page pref: wrapped init `navigateTo` in `requestAnimationFrame` to fix jumps not rendering on load
- Guard in app.js: added `throw new Error()` after redirect so null `currentUser` doesn't crash downstream code
- Dropdown hover: removed padding from `.user-dropdown`, `overflow:hidden`, items fill full width
- Dropdown divider: `margin: 0` (was `4px 0`)
- Stats page centering: `.stats-wrap { max-width:720px; margin:0 auto }`, `.acct-grid` had duplicate definition fighting the center
- Jumps page horizontal scroll: `columns-area` owns `overflow-x:auto`, page-content gets `jumps-page` class added/removed on navigate

### Morning Briefing Cron
- Old cron errored — deleted and recreated (ID: `fb0bdf23-3287-4962-aab0-6421112a0b68`)
- Brave Search API returning 422 invalid token — using Coinbase + Yahoo Finance + Open-Meteo instead

### MSTR Options Cron
- Recreated with CBOE data source (ID: `3e55a8dd-5dfa-4c1d-8025-7388d8c570c6`)
- CBOE endpoint: `https://cdn.cboe.com/api/global/delayed_quotes/options/MSTR.json` — no auth needed, 15-min delayed
- Option ticker parsing: `MSTR260306C00030000` → regex extracts exp date, type (C/P), strike (÷1000)
- Fires 9:35am EST Mon–Fri, skips US market holidays

## JumpKit Current State
- App entry: `Topics/JumpKit/app/index.html` → `app.html`
- All data in localStorage (file-based DB attempt was reverted — broke the app)
- Click logging: `DB.logClick()` called in `incrementClick()`, stored as `jk_clicks_${userId}`
- Jump click handler: shows `alert()` placeholder + toast + increments count
- Toast: success (green ✓) and danger (red !) — centered top, 3s auto-dismiss, defined in app.js as IIFE
- Stats charts use bundled Chart.js (`js/chart.min.js`, 205KB)

---

## Session Continued (Thu Mar 5, afternoon/evening)

### JumpKit UI Polish

#### Jumping Man Icon
- Downloaded `jump-icon.svg` + `jump-icon.png` from Jeff's Downloads folder
- Copied to `app/assets/`
- Used inline in Jumps nav button: `fill="currentColor"`, 18×18px, `.jump-man-icon` CSS class
- Also used on Home page "Add Your First Jump" tip card (1.5rem)

#### Card & UI Fixes
- Card borders changed from turquoise (`rgba(0,194,199,0.12)`) → subtle gray (`rgba(255,255,255,0.09)` dark / `rgba(0,0,0,0.10)` light)
- Jump columns: `box-shadow: 0 2px 8px rgba(0,0,0,0.18)` — soft gray shadow
- Home page tip cards + Help page cards: icon and title now on same line (`display:flex; align-items:center; gap:10px`), both `1.05rem` title / `1.5rem` icon

#### Dropdown Fixes
- Global `mousedown` listener on `document` closes all `.custom-select-menu.open` dropdowns — replaces broken per-dropdown self-removing listeners
- `e.stopPropagation()` removed from all trigger clicks (was causing race condition)
- Works for: Add Jump modal column picker, Settings starting page, Settings auto-archive

#### Stats Page Slider
- Rebuilt to match jumps page slider exactly (event delegation, `offsetLeft`/`offsetWidth` positioning)
- Last tab (Yearly) pill stretches flush to right edge: `isLast ? bar.offsetWidth - active.offsetLeft : active.offsetWidth`
- `stats-filter-bar` class removed (was causing visual issues); now uses same classes as jumps slider

#### Stats View Data Ranges (final)
- Daily: this week, one bar per day (Sun–Sat)
- Weekly: last 52 calendar weeks, one bar per week, labels every 4th week
- Monthly: this year Jan–Dec, one bar per month
- Yearly: last 4 complete years + current year YTD (5 bars)

#### Settings Page Section Icons (final)
- Preferences: `ti-adjustments-horizontal`
- Productivity: `ti-rocket`
- Maintenance: `ti-tool`
- All section title icons: `1.1rem` via `.acct-section-title i`

#### Help Page
- How-To Tips: `ti-list-check`
- Features: `ti-layout-grid`
- FAQs: `ti-message-circle` (unchanged)

#### Nav Icon (Jumps)
- Custom inline SVG jumping man (from downloaded asset), `fill="currentColor"`, 18×18px
- Also in My Account `app/js/app.js` nav bar as inline SVG

#### Horizontal Scrollbar (Jumps Page)
- `.page-content.jumps-page`: `overflow:hidden`, `display:flex`, `flex-direction:column`
- `.columns-area`: `overflow-x:auto`, `flex:1`, `min-height:0`, `margin-bottom:14px`
- Custom scrollbar: `height:7px`, styled with `var(--border-input)`, turquoise on hover

#### User Dropdown
- `padding: 0; overflow: hidden` — hover fills full width, no top/bottom gap
- Divider: `margin: 0` (removed 4px top/bottom margin)

### Electron Plan Discussed
- macOS: scaffold main.js → swap localStorage→SQLite → wire shell.openExternal → globalShortcut → tray icon → electron-builder (universal .dmg) → notarize → electron-updater
- Windows: identical code, different packaging — NSIS installer, EV code signing cert, .ico icon
- File structure agreed: `main.js`, `preload.js`, `assets/icon.icns + icon.ico`, `build/` output
