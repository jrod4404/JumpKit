# Memory — 2026-06-20

## JumpKit Security Audit & Fixes

### Security items addressed
- **#12 setWindowOpenHandler URL validation**: Discussed with Jeff — jump launcher correctly uses `shell.openPath()` for local files. `RISKY_EXTENSIONS` regex was dead code (never wired up). Jeff decided to add risky-extension confirmation modal as a **future feature** (tracked in JUMPKIT_DOCS.html). No code change made for this item.
- **#11 CSP hardening**: Added `object-src 'none'; base-uri 'self'` to Content-Security-Policy in `app/main.js`.
- **#28 ls-webhook signature verification**: Added Lemon Squeezy webhook signature verification using `LEMON_SQUEEZY_SIGNING_SECRET` in `supabase/functions/ls-webhook/index.ts`.
- **#8 sandbox enablement**: `sandbox: true` added to BrowserWindow in `app/main.js`.
- **#26 apply-pending-upgrade auth**: Hardened to require authenticated requests.
- **#27 CORS tightening**: Removed wildcard origins from relevant endpoints.

### Key decision: risky extensions
- `.app`, `.exe`, `.sh`, `.bat`, `.cmd`, `.pkg`, `.dmg`, `.scpt` are currently NOT blocked or gated — they open immediately for personal jumps
- Shared team jumps to local paths DO show a confirmation modal
- Future feature: add confirmation modal for risky extensions on personal jumps too (tracked in JUMPKIT_DOCS.html)

## JumpKit Tests

### Test fixes this session
- **Test 382**: Confirmed `platforms: ['win']` — Windows only ✅
- **Test 23**: Fixed `renderAdmin is not defined` error by loading `js/admin.js` via `loadScript()` before calling `renderAdmin()`
- **Mark as Skipped bug**: Two bugs fixed:
  1. `_buildTestDetailContent` had no `state === 'skip'` branch → added proper branch with gray minus icon + "Skipped" label
  2. "Mark as Skipped" button remained visible after already skipped → now hides when state is already `skip`
- **Test 376 prompt**: Jeff asked to update end of prompt wording (assistant turn failed before completing — may need to retry)

### Testing session state
- Mac preflight tests: **DONE** (Jeff confirmed at ~09:58 EDT)
- Test 23: **FAILING** with `renderAdmin is not defined` (fix applied this session)
- Windows testing: still locked until Mac run fully finalized

## JumpKit localStorage/sessionStorage Audit
- Approved dynamic keys: `jk_notifs_{userId}`, `jk_downgrade_notif_{userId}`
- Logout clears `jk_session_token`
- DB audit item #4 resolved: passwords no longer stored in `jk_users`
- All `console.log`/`console.debug` removed from production JS
- `.bak` files deleted
- `.limit(1000)` added to `shared_jumps` query

## Safari Tabs Recovery
- Jeff had ~10 tabs open yesterday (June 19) and lost them
- Recovered from Safari history and reopened ~8 tabs:
  - Claude Platform / Cost
  - Apple Developer: Programs, Enterprise Program, Account Info, Migrate Individual → Org, D-U-N-S Lookup
  - Tabler Icons
  - Proton Mail inbox

## JumpKit Release Checklist Reminder
- Must add: "Confirm Lemon Squeezy is in non-test mode" to deployment checklist
- Mac testing must complete before Windows unlocks
- Release HTML export must include full metadata header + mirror in-app Testing layout

## Open TODOs (JumpKit)
- [ ] Retry test 376 prompt update (previous assistant turn failed)
- [ ] Finalize Mac testing session, then unlock/run Windows
- [ ] Clear-session modal icon color fix (match header text color)
- [ ] Add jk_ metadata & changelog tabs to Release HTML
- [ ] Re-export release HTML to verify metadata header + UI parity
- [ ] Add "Confirm Lemon Squeezy is non-test mode" to deployment checklist
- [ ] Draft Supabase dev-project duplication plan (post-v1)
- [ ] Future: risky-extension confirmation modal for personal jumps

## Skipped pill / Testing Summary
- Skipped state now fully handled: pill shows in summary card, "Mark as Skipped" btn hides after skipping, `_buildTestDetailContent` shows proper gray/minus UI
- Pill order in Testing Summary: Release HTML → Mac → Win
- Section pills only appear when totals > 0
- TO DO counts must show in Testing Summary card
# Memory — 2026-06-20

## JumpKit Security Audit & Fixes

### Security items addressed
- **#12 setWindowOpenHandler URL validation**: Discussed with Jeff — jump launcher correctly uses `shell.openPath()` for local files. `RISKY_EXTENSIONS` regex was dead code (never wired up). Jeff decided to add risky-extension confirmation modal as a **future feature** (tracked in JUMPKIT_DOCS.html). No code change made for this item.
- **#11 CSP hardening**: Added `object-src 'none'; base-uri 'self'` to Content-Security-Policy in `app/main.js`.
- **#28 ls-webhook signature verification**: Added Lemon Squeezy webhook signature verification using `LEMON_SQUEEZY_SIGNING_SECRET` in `supabase/functions/ls-webhook/index.ts`.
- **#8 sandbox enablement**: `sandbox: true` added to BrowserWindow in `app/main.js`.
- **#26 apply-pending-upgrade auth**: Hardened to require authenticated requests.
- **#27 CORS tightening**: Removed wildcard origins from relevant endpoints.

### Key decision: risky extensions
- `.app`, `.exe`, `.sh`, `.bat`, `.cmd`, `.pkg`, `.dmg`, `.scpt` are currently NOT blocked or gated — they open immediately for personal jumps
- Shared team jumps to local paths DO show a confirmation modal
- Future feature: add confirmation modal for risky extensions on personal jumps too (tracked in JUMPKIT_DOCS.html)

## JumpKit Tests

### Test fixes this session
- **Test 382**: Confirmed `platforms: ['win']` — Windows only ✅
- **Test 23**: Multiple fix iterations this session:
  1. First fix: loaded `js/admin.js` via `loadScript()` before calling `renderAdmin()` → fixed `renderAdmin is not defined`
  2. Second error: `loadScript helper not available` — Test 23 runs in a context where loadScript isn't accessible
  3. Current fix applied: Replaced `el.innerHTML = savedHTML` page restore with `navigateTo('tests')` call, which re-renders the full page with all event listeners re-wired. This also fixes the "Save Results button not working" bug after the test ran.
  4. Root cause of save button issue: `el.innerHTML = savedHTML` recreated DOM nodes without re-attaching event listeners; `navigateTo('tests')` properly re-renders via `renderTests()`.
- **Mark as Skipped bug**: Two bugs fixed:
  1. `_buildTestDetailContent` had no `state === 'skip'` branch → added proper branch with gray minus icon + "Skipped" label
  2. "Mark as Skipped" button remained visible after already skipped → now hides when state is already `skip`
- **Section pill count mismatch (release.html vs app)**: Fixed bug in `_saveReleaseSection` where `mode === 'auto'` filter was missing `!t.preflight` guard — preflight tests were being included in the auto section's save, causing count mismatch. Fix: added `!t.preflight` to auto filter.

### Testing session state
- Mac preflight tests: **DONE** (Jeff confirmed at ~09:58 EDT)
- Test 23: Fix applied (navigateTo approach) — needs verification by Jeff
- Save Results buttons: Should work again after Test 23 fix
- Release HTML vs app pill mismatch: Fixed (sectionTests filter corrected)
- Windows testing: still locked until Mac run fully finalized

## JumpKit localStorage/sessionStorage Audit
- Approved dynamic keys: `jk_notifs_{userId}`, `jk_downgrade_notif_{userId}`
- Logout clears `jk_session_token`
- DB audit item #4 resolved: passwords no longer stored in `jk_users`
- All `console.log`/`console.debug` removed from production JS
- `.bak` files deleted
- `.limit(1000)` added to `shared_jumps` query

## Safari Tabs Recovery
- Jeff had ~10 tabs open yesterday (June 19) and lost them
- Recovered from Safari history and reopened ~8 tabs:
  - Claude Platform / Cost
  - Apple Developer: Programs, Enterprise Program, Account Info, Migrate Individual → Org, D-U-N-S Lookup
  - Tabler Icons
  - Proton Mail inbox

## JumpKit Release Checklist Reminder
- Must add: "Confirm Lemon Squeezy is in non-test mode" to deployment checklist
- Mac testing must complete before Windows unlocks
- Release HTML export must include full metadata header + mirror in-app Testing layout

## Open TODOs (JumpKit)
- [ ] Verify Test 23 passes with navigateTo fix (Jeff to confirm after npm start restart)
- [ ] Retry test 376 prompt update (previous assistant turn failed)
- [ ] Finalize Mac testing session, then unlock/run Windows
- [ ] Clear-session modal icon color fix (match header text color)
- [ ] Add jk_ metadata & changelog tabs to Release HTML
- [ ] Re-export release HTML to verify metadata header + UI parity
- [ ] Add "Confirm Lemon Squeezy is non-test mode" to deployment checklist
- [ ] Draft Supabase dev-project duplication plan (post-v1)
- [ ] Future: risky-extension confirmation modal for personal jumps

## Skipped pill / Testing Summary
- Skipped state now fully handled: pill shows in summary card, "Mark as Skipped" btn hides after skipping, `_buildTestDetailContent` shows proper gray/minus UI
- Pill order in Testing Summary: Release HTML → Mac → Win
- Section pills only appear when totals > 0
- TO DO counts must show in Testing Summary card
