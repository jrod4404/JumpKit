# Memory — 2026-04-02

## JumpKit: Shared Jumps — Bug Fixes Completed

### Root Cause Chain (fully resolved)
- `DB.saveJumps` didn't exist → replaced with `DB.updateJump`/`DB.createJump` in sync.js
- `createJump` ignored passed `id` → fixed to accept pre-set id, isShared, teamId, supabaseId
- `supabaseId` not in SQLite schema for columns or jumps → added via `addColumnIfMissing` in main.js
- `save-jump` IPC handler didn't include `supabaseId` in INSERT → fixed
- `save-column` IPC handler didn't pass `supabaseId` values → fixed
- Unique constraint added to `shared_columns (team_id, created_by, name)` to prevent future dupes
- `syncColumnToSupabase` updated to upsert on `team_id,created_by,name` conflict
- Sync receiver dedupes remote cols by `(team_id + name)` keeping latest
- Stale local shared columns removed during sync if no longer in remote
- Sync receiver preserves local column `order` (never overwrites from remote) — each member controls their own layout

### Shared Jump Edit Permissions
- **Owner**: full Edit/Archive/Delete on shared jumps via right-click context menu; edits push back to Supabase via `shared_jumps` UPDATE
- **Team member**: Set Hotkey + Add/Remove Favorite only (personal, not synced)

### Verified Working
- Bosch account sees exactly one Team 1 Share column with jump inside
- `syncSharedJumps()` logs "Synced 1 columns, 1 jumps"
- Jump in Supabase: `f9b2ac0c-4be0-450a-b2ea-61a82401653b` "Mac Desktop" → `/Users/jeffroder/Desktop`

---

## Documentation Created
- `JUMPKIT_DOCS.md` — comprehensive developer doc, 55KB, all 10 sections
- `JUMPKIT_DOCS.html` — single-page styled HTML version with dark sidebar TOC, JumpKit colors, opened in browser
- Path: `/Users/jeffroder/.openclaw/workspace/Topics/JumpKit/`

---

## System Changes
- `~/.openclaw` ownership changed: `sudo chown -R jeffroder:staff` + `chmod -R 755` — no longer root-owned, Finder accessible
- `/Applications/Ollama.app` ownership changed to jeffroder:staff
- **OpenClaw default model changed to Sonnet**: `agents.defaults.model.primary = "anthropic/claude-sonnet-4-6"`, fallback = haiku
  - File: `/Users/jeffroder/.openclaw/openclaw.json` line 107

---

## Ollama Local Models Tested
- `gemma4:e4b` (9.6GB) — fast, working ✅
- `gemma4:26b` (17GB) — slower (~30-60s first token), working ✅
- Both accessible at `http://localhost:11434`
- Jeff wants to test `gemma4:e4b` as OpenClaw primary model
- Config change made to `ollama/gemma4:e4b` but session still on Sonnet (session persists until /compact)
- To switch back: set `primary` back to `"anthropic/claude-sonnet-4-6"` in openclaw.json line 107

---

## Pending
- Jeff testing gemma4:e4b as OpenClaw model after /compact resets session
- Remaining JumpKit TODOs: account page UI cleanup, teams page UI cleanup, Resend email setup, unit tests, code check (items 14, 15, 17)
