# 2026-05-07 — Session Notes

## Windows Installer Build
- Fixed Wine permission issues blocking installer creation:
  - `sudo chmod -R +x ~/Library/Caches/electron-builder/wine/` (then chown back)
  - `sudo chown -R jeffroder ~/Library/Caches/electron-builder/`
- Successfully built fresh installer: `dist/JumpKit Setup 1.0.0.exe` (96MB, May 7 07:40)
- Path: `/Users/jeffroder/.openclaw/workspace/Topics/JumpKit/app/dist/JumpKit Setup 1.0.0.exe`

## Landing Page Updates

### Hero Section
- Replaced old macOS mock windows with real screenshots
- macOS: `hero-mac-dark.jpg` / `hero-mac-light.jpg` (new JPG images, May 7)
- Windows: `hero-windows-dark.jpg` / `hero-windows-light.jpg` (updated multiple times today)
- Both stacked vertically, theme-aware (CSS `[data-theme]` switching)
- Removed 3D perspective transform to fix blurriness — now flat with box-shadow glow
- Added hover lift effect (`translateY(-4px)`)
- Added lightbox on click — opens full-size with dark overlay + turquoise glow, Esc to close
- Lightbox is theme-aware (opens correct dark/light image)
- `cursor: zoom-in` on images
- Desktop: `padding-top: 53px` on hero-visual; mobile: `padding-top: 0`

### Solution Section
- Replaced plain button with clickable demo thumbnail:
  - Shows macOS screenshot with semi-transparent overlay + turquoise play button circle
  - Click launches Supademo (`cmopzrg0m0510xa0jfqglvrfv`)
  - Theme-aware: shows dark/light mac screenshot
  - Play button color: `#00a8c8` (matches scroll-to-top and theme toggle)
  - Overlay text removed, play button only

## Git
- Landing latest commit: `326e5a8`
- All changes pushed to GitHub → auto-deployed via Vercel

## TODO (carry forward)
- [ ] Verify Vercel SSL cert resolved for jumpkit.app
- [ ] Audit tests 46–51 — team sharing Supabase + SQLite sync
- [ ] Live invite email test end-to-end via Resend
- [ ] Account + Teams page UI cleanup (consider merging)
- [ ] Wire "Get JumpKit Free Tier" button → Lemon Squeezy
- [ ] Security + vibe coding audit + backup
- [ ] Once jumpkit.ai verified in Resend → swap from/to in send-feedback edge function

---

## 2026-05-10 Updates

### Security Fixes Applied
- CORS locked to `https://jumpkit.app` on `send-feedback.js` and `waitlist-signup` Edge Functions
- Rate limiting added to `send-feedback` (5 req/min) and `send-invite` (10 req/min)
- Team password hashing upgraded from SHA-256 → PBKDF2 (100,000 iterations)
- Tests 54–62 added covering all 9 security checklist items
- DevTools disabled in production builds (Test 53)

### ⚠️ IMPORTANT: Password Hash Migration Note
- Team passwords were previously hashed with plain SHA-256
- Now using PBKDF2 (much stronger) — **hashes are NOT backwards compatible**
- Any existing teams in Supabase with SHA-256 hashed passwords will fail verification
- **Action needed before launch:** Either:
  1. Clear all test team password hashes in Supabase (`teams` table → `team_password_hash` column) and force re-entry, OR
  2. Add a `hash_algo` column to `teams` table to support gradual migration (old = sha256, new = pbkdf2)
- Pre-launch this only affects test data — safe to just delete and re-create test teams
- Post-launch: must do proper migration to avoid locking out existing team owners

### TODO (carry forward — updated)
- [ ] Live invite email test end-to-end via Resend
- [ ] Account + Teams page UI cleanup (consider merging)
- [ ] Wire "Get JumpKit Free Tier" button → Lemon Squeezy
- [ ] Once jumpkit.ai verified in Resend → swap from/to in send-feedback edge function
- [ ] ⚠️ Team password hash migration — clear SHA-256 hashes before launch, force re-entry
- [ ] Rebuild Windows installer with latest code before any distribution

---

## 2026-05-10 Deployment Audit Notes
- SSL ✅ valid, HTTPS enforced, HSTS set (2 years)
- Env vars ✅ set via Supabase Edge Function secrets
- Firewall ✅ N/A (Vercel + Supabase managed)
- Process manager ✅ N/A (no self-hosted server)

### ⚠️ Action: Verify LEMON_SQUEEZY_SIGNING_SECRET in Supabase
- Go to Supabase dashboard → Edge Functions → ls-webhook → Secrets
- Confirm LEMON_SQUEEZY_SIGNING_SECRET is set
- If missing, Lemon Squeezy webhook verification will silently fail
