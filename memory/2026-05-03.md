# 2026-05-03 — Session Notes

## Infrastructure
- Supabase free tier had paused the JumpKit project over the weekend — Jeff resumed it manually, back online by ~7:55 AM
- jumpkit.app DNS is correct (all 3 Vercel A records in GoDaddy), landing page renders via HTTP
- Vercel still showing "invalid configuration" for SSL cert as of morning — Jeff hit Refresh, pending resolution
- No vercel.json in landing repo (confirmed)

## JumpKit Landing Page — Major Updates Today

### Hero Section
- Replaced both mock window HTML elements with real app screenshot
- Dark mode: `assets/Jumpkit-hero.png`
- Light mode: `assets/Jumpkit-hero-light.png`
- CSS uses `[data-theme]` attribute to swap images automatically
- Screenshot styled with 3D tilt effect (perspective rotateY/X), turquoise glow drop-shadow, hover flattens
- Mobile: tilt removed, margin-top: 0, margin-bottom: 32px
- Hero inner-wrap changed to `align-items: center` for vertical centering

### Problem Section (new)
- Added after hero, before Features
- Title: "Bookmarks Don't Cut It"
- 6 painpoint cards in features-grid layout with compact padding (14px 20px)
- Icons: `thumb-down-off` in muted red (#e05555)
- Section label uses `alert-circle` icon

### Solution Section (new)
- Added after Problem section
- Title: "Jumpkit"
- Subtitle: "One app to store, launch, and share your navigation data without anyone ever changing it."
- Contains Supademo "Try the Interactive Demo" button (ID: cmopzrg0m0510xa0jfqglvrfv)
- Removed standalone "Live Demo" section

### Nav Updates
- Desktop hero-section-nav: 7 links in 2 rows (flex-wrap, max-width 520px)
  - Home, The Problem, The Solution, Features, Dashboard, How It Works, Pricing
- Mobile offcanvas: same links with icons

### Style Tweaks
- Section title size reduced: `clamp(1.6rem, 3vw, 2.4rem)`
- `section-sub` now globally centered with `margin: 0 auto` and `text-align: center`
- Light mode text softened: `--text: #2E3D4F`, `--text-muted: #6B7E93`
- Problem + Solution sections added to CSS centering rules

### Supademo
- Script: `https://script.supademo.com/supademo.js` (loaded in `<body>`)
- Demo ID: `cmopzrg0m0510xa0jfqglvrfv`
- Shows adding a hyperlink jump + directory jump with background music

## Git
- Landing repo: multiple commits pushed to GitHub → auto-deployed via Vercel
- Latest commit: `7842572`

## TODO (carry forward)
- [ ] Verify Vercel SSL cert resolved for jumpkit.app (was pending)
- [ ] Audit tests 46–51 — team sharing Supabase + SQLite sync
- [ ] Live invite email test end-to-end via Resend
- [ ] Account + Teams page UI cleanup (consider merging)
- [ ] Wire "Get JumpKit Free Tier" button → actual download or Lemon Squeezy
- [ ] Security + vibe coding audit + backup
- [ ] Once jumpkit.ai verified in Resend → swap from/to addresses in send-feedback edge function
- [ ] Add Windows screenshot of JumpKit to hero section
- [x] Add industry icons and cards to hero section
- [x] Add two more cards to Features section
