# Memory — 2026-05-27

## JumpKit Security Audit — Session Progress

Worked through CODE_REVIEW.md audit items with Jeff. All 🔴 Critical and 🟠 High items are now closed.

### Completed Today

**#1 — team_password_plain removed** ✅
- Dropped `team_password_plain` column from Supabase (SQL run by Jeff)
- Removed all writes of plaintext password from `teams.js`
- `verify-team-password` Edge Function deployed
- Invite flow: owner enters password → verified server-side → plaintext included in email only
- Tested and confirmed working

**#2 — Client-side hash comparison closed** ✅
- `joinTeam()` already using `verify-team-password` Edge Function (done in prior session)
- Additional fix: replaced all `select('*')` on `teams` table with explicit column list
- `team_password_hash` never leaves the server in any code path
- 4 locations fixed: lines 113, 120, 537, 1054 in teams.js

**UI fix — Invite Members modal** ✅
- Header icon changed from `ti-mail` → `ti-user-plus`
- Removed "(verified server-side & included in invite email)" label text

**#3 — sync-jumps SQL column/value mismatch** ✅
- Added missing `@supabaseId` to VALUES list (9 columns, was only 8 values)

**#4 — log() undefined in main.js** ✅
- Jeff decided to remove all console.log entirely (not replace with log helper)
- Removed `log()` definition and all 3 call sites in migrateUserId + seedNewUser

**#5 — CSP tightened** ✅
- Split into proper directives, removed `unsafe-eval`, `unsafe-inline` from script-src
- Created `js/theme-bootstrap.js` (moved inline theme script)
- Moved pw-toggle inline script into `auth.js`
- Added `cdn.jsdelivr.net` to `connect-src` for source maps
- Added `'unsafe-inline'` back to `script-src` temporarily (inline onclick handlers) — noted that #11 (event delegation) will fully remove it

**#6 — requestSingleInstanceLock()** ✅
- Added single instance lock to prevent two JumpKit processes opening same SQLite DB
- Second launch focuses existing window instead of opening duplicate

**#7 — shell.openPath validation** ✅
- Personal jumps: open anything, no restrictions
- Shared team jumps with local path: native confirmation dialog
- Dialog text: "This jump was shared by your team and points to a local path: [path]. As a security precaution, is this the file you actually want to open?"
- Passes `{ url, isShared }` from renderer; updated preload.js and jumps.js

**#8 — hardenedRuntime: true** ✅
- Set in package.json — required for Apple notarization / Gatekeeper

**#9 — setWindowOpenHandler + will-navigate lockdown** ✅
- External window.open() redirected to system browser, new BrowserWindow denied
- will-navigate blocks non-file:// navigation

**Electron Fuses** ✅ (bonus, not in original audit)
- Created `fuse.js` afterPack hook
- Disables --inspect, NODE_OPTIONS, RunAsNode
- Enables cookie encryption, asar integrity validation
- Only applies to production builds

### Debugging Session — Shared Jump Sync
- Discovered "JumpKit Docs" wasn't appearing on support@jumpkit.ai — traced to one-time Supabase connection hiccup at original creation
- Added temporary `[sync]` and `[saveJump-create]` debug logging, confirmed both create and edit/reassign paths work correctly
- Confirmed sync fires on login and every 2 minutes
- Removed all debug logging after confirmation
- "renderColumns not available" log during startup sync is benign — user isn't on jumps page at that moment, navigating there always re-renders with fresh data

### Docs & Backup
- Added Section 10 "Security Architecture" to `JUMPKIT_DOCS.html`
- Backup created: `app_backups/app_backup_2026-05-27`

---

## Remaining Medium Items (#10–#19)

| # | Issue |
|---|---|
| 11 | Event delegation refactor — 80+ inline onclick → unlocks full CSP script-src lockdown |
| 10 | tests.js (130KB) ships to all users |
| 12 | chart.min.js (200KB) loaded on every page |
| 13 | auth.js hardcodes anon key JWT literal |
| 14 | Admin check uses hardcoded email |
| 15 | isShared mixed 1/0 vs boolean in sync layer |
| 16 | Stale data cleanup two ID schemes → ghost rows |
| 17 | Dead code in main.js (globalShortcut, tray) |
| 18 | Auto-seed triple-guard race |
| 19 | getColumns sort inconsistency IPC vs cache |

Low (#20, #21) — decided to skip or batch with housekeeping.

---

## Test Accounts

- jeffroder@gmail.com — team owner
- support@jumpkit.ai — team member
- E2E-Test-Team password: `TestPass123!`
- JeffTestOrg-Team1 password: `JeffTestOrg-Team1!`
- JeffTestOrg-Team2 password: `JeffTestOrg-Team2@`

---

## JumpKit General Notes

- June 2026 target to complete: Jump organizer, launcher features, OpenClaw wrapper features, UI, landing page
- Marketing plan: online forums, email broadcasts, IT MSP partners (commission-based)
- Proton Mail: Jeff has jumpkit.ai suffix on Proton Plus. Has 1 of 1 domains used. Would need second Proton account for jumpkit.app suffix.
