
## JumpKit DB audit change plan — 2026-06-25
- Jeff accepted audit item #6 (auto-backup behavior) as-is for now and asked to skip item #2 (dev/prod Supabase separation) for now.
- Added TodoKit JumpKit task `auto_db_audit_y2y1yog2` titled "Fix database audit release blockers" with no code implementation.
- Plan covers: untrack secrets/temp files + rotate Apple password if pushed, sanitize logs, tighten SQLite IPC scoping, and whitelist SQLite migrations.

## JumpKit DB audit blockers implemented — 2026-06-25
- Implemented approved DB audit items 1-4: ignored/untracked Supabase `.temp`, sanitized selected sensitive logs, whitelisted SQLite migrations, and tightened SQLite IPC scoping for shared-jump delete, click-log backfill, and sync state.
- `app/.env` is not tracked in current git history (`git log --all -- app/.env` returned no commits); Supabase `.temp/pooler-url` has commit history and is now staged for removal from git tracking while local files remain.
- Verification: `npm test` in `Topics/JumpKit/app` passed 28/28.
## JumpKit DB audit and release blocker fixes — 2026-06-25
- Jeff asked for a full JumpKit database audit covering local SQLite (`app/js/db.js`, `app/preload.js`, Electron IPC in `app/main.js`) and Supabase/Postgres (`app/supabase/`, Edge Functions). Audit result: mostly sound, but release blockers found around tracked secret/temp files, sensitive logging, SQLite IPC scoping, and dynamic migration SQL. Jeff said to skip dev/prod Supabase separation for now and accept backup behavior as-is.
- Added JumpKit TodoKit task `auto_db_audit_y2y1yog2` (“Fix database audit release blockers”) with a plan; later completed it after Jeff approved items 1-4.
- Implemented fixes: added ignore rules for `app/.env`, `.env.*`, and `app/supabase/.temp/`; untracked Supabase temp files with `git rm --cached` while keeping local copies; confirmed `app/.env` had no git history in current repo so no Apple password rotation was performed.
- Sanitized selected logs to avoid emails/profile IDs/team IDs in console/Edge Function output.
- Replaced dynamic SQLite migration identifier interpolation with whitelisted/fixed migrations.
- Tightened SQLite IPC scoping: `delete-shared-jumps` and `log-click-name` now require `userId`; `sync_state` supports user-scoped keys with legacy fallback.
- Verification after DB blocker fixes: JS syntax checks passed for touched JS files; `npm test` passed 28/28. Caveat: shared/team sync UI still should be manually checked because IPC scoping touched runtime behavior.
- TodoKit task `auto_db_audit_y2y1yog2` marked Completed with comment: “Implemented DB audit blockers 1-4; npm test passes. Jeff to check.”

## JumpKit Home jump count discrepancy — 2026-06-25
- Jeff saw Home Jumps card report 46 while visible columns summed to 41.
- SQLite inspection found active counts: 41 valid-column jumps + 10 orphan shared/team jumps whose `columnId` pointed to missing local columns (`cc07895b-f584-4122-a0fd-7882e9af54b9`, `de550b04-0238-4bb6-9f93-1fa42f4367fc`). The live app likely had one 5-jump orphan batch in memory, explaining 46.
- Made backup `/Users/jeffroder/Library/Application Support/jumpkit/jumpkit.db.pre-orphan-cleanup-20260625-083928` before cleanup.
- One-time SQLite cleanup removed orphan active shared/team jumps; final active count for user `6c0763f4-21d4-470a-bc10-1e6eafa3873a` is 41 and orphan count is 0.
- Code guard added: `DB._removeOrphanSharedJumps(userId)` removes active orphan shared/team jumps during init; `DB.getRenderableActiveJumps(userId)` counts only active jumps with valid local columns; Home card now uses renderable active jumps.
- Verification: `node --check js/db.js`, `node --check js/app.js`, and `npm test` passed (28/28).

## JumpKit orphan jump prevention audit — 2026-06-25
- Jeff suspected backup import could create orphan jumps. Checked import path plus shared-sync and SQLite IPC paths.
- Import path already maps selected active backup jumps to local selected columns, but hardened it: archived import now only reuses a personal Archived column (not shared/team), and `doImport` skips if target column is missing.
- Added DB-level invariant guards in `DB.createJump` and `DB.updateJump`: any jump create/update with a missing/invalid `columnId` is blocked before cache/SQLite persistence.
- Shared sync was the more likely orphan source: renderer and IPC used `rj.shared_column_id` as a fallback columnId if no local column mapping existed. Fixed sync to skip remote shared jumps unless the Supabase shared column maps to a real local column, and changed IPC `upsert-shared-jumps` to verify `(columnId,userId)` exists before SQLite upsert.
- Preserved `supabaseId` on IPC shared-jump upsert so stale deletion can match remote IDs reliably.
- Verification: syntax checks passed for `app/main.js`, `app/js/db.js`, `app/js/sync.js`, `app/js/app.js`; `npm test` from `Topics/JumpKit/app` passed 28/28; current SQLite orphan count remains 0.

## Pre-compaction flush — JumpKit DB/orphan jump work
- Jeff reported Home page Jumps card showing 46/51 active jumps while visible columns totaled 41. Investigation found local SQLite had orphan active shared/team jumps whose `columnId` referenced missing local columns.
- Cleaned local DB after backup: `/Users/jeffroder/Library/Application Support/jumpkit/jumpkit.db.pre-orphan-cleanup-20260625-083928`. Verified after cleanup: 41 active jumps, 0 active orphan jumps.
- Added app guards/fixes to prevent recurrence:
  - `DB._removeOrphanSharedJumps(userId)` cleanup for active shared/team jumps with missing columns.
  - Home card now uses renderable active jumps only via `DB.getRenderableActiveJumps()`.
  - `DB.createJump()` blocks invalid/missing `columnId`; `DB.updateJump()` blocks moving jumps to invalid/missing `columnId`.
  - Shared sync now skips remote shared jumps unless shared column maps to a real local column.
  - SQLite IPC `upsert-shared-jumps` verifies target `(columnId,userId)` exists before writing and preserves `supabaseId` for stale cleanup.
  - Backup import hardened: archived import only reuses a personal Archived column; import skips a jump if target column missing.
- Verified syntax checks passed for `app/main.js`, `app/js/db.js`, `app/js/sync.js`, `app/js/app.js`; test suite passed 28/28; SQLite orphan count 0.
- Jeff should refresh/restart JumpKit to load runtime guards.

## JumpKit Jumps page subtitle polish — 2026-06-25
- Jeff asked to update the subtle help text at the top of the Jumps page to include archived jump count and replace “clicks” with “launches.”
- Patched `app/js/jumps.js:getStatsText()` active-filter text to show `N active · N archived · N launches · N min saved`, using `DB.getRenderableActiveJumps()` when available.
- Patched `app/js/app.js:getStatsHTML()` for consistency: active stats summary includes archived count and uses renderable active jumps.
- Verification: `node --check js/jumps.js`, `node --check js/app.js`, and `npm test` passed (28/28).

## JumpKit stats/configure columns polish — 2026-06-25
- Jeff requested three UI fixes: stats page total time saved decimal places match Jumps page subtitle, Configure Columns show number of jumps per column, and personal columns show read-only Sharing value “Not shared” instead of looking editable.
- Patched `app/js/app.js:renderStatsDash()` time saved cards to show one decimal place (`toFixed(1)`) instead of rounded integer minutes.
- Patched `app/js/jumps.js:_makeColConfigRow()` to add a discrete Jumps count column and change personal sharing status to a read-only “Not shared” value.
- Patched `app/css/app.css` column config grid and added `.col-readonly-value` / `.col-jump-count` styles.
- Verification: `node --check js/app.js`, `node --check js/jumps.js`, and `npm test` passed (28/28).

## JumpKit Configure Columns modal width/alignment polish — 2026-06-25
- Jeff requested Configure Columns modal 15% wider, stable horizontal alignment inside drag/drop cards, ellipsis for long sharing team names, and “Not shared” directly under SHARING.
- Patched `app/js/jumps.js` to open Configure Columns with `lg config-cols-modal`, wrap team names in `.col-status-text` for ellipsis, and render personal sharing as plain read-only “Not shared” text (no icon offset).
- Patched `app/css/app.css`: `.modal-box.config-cols-modal { max-width: 782px; }` (15% wider than 680px lg), grid uses `minmax(0,...)`, sharing field clips overflow, and `.col-status-text` ellipsizes long names.
- Verification: `node --check js/jumps.js` and `npm test` passed (28/28).

## JumpKit Configure Columns sharing direction badges — 2026-06-25
- Jeff approved adding clearer sharing direction in Configure Columns.
- Patched `app/js/jumps.js` sharing badges to show `Shared out · Team Name` for owned/team-owner shared columns and `Shared in · Team Name` for columns shared into JumpKit as a team member. Added directional Tabler icons and explanatory tooltips.
- Patched `app/css/app.css` with `.col-status-dir`, `.col-status-dot`, and stronger shared-out tint while preserving ellipsis on team names.
- Verification: `node --check js/jumps.js` and `npm test` passed (28/28).

## JumpKit sharing pill and team pill polish — 2026-06-25
- Jeff requested Configure Columns sharing pills: less left padding before Shared out/in, use arrow separators instead of dot, unbold Shared out/in text, subtle green for Shared in to align with Member green, and recolor Team page Unlimited pill to CTA gradient.
- Patched `app/js/jumps.js`: pills now render `Shared out → Team Name` and `Shared in ← Team Name`.
- Patched `app/css/app.css`: tighter left padding, `.col-status-dir` unbolded, `.col-status-arrow` added, Shared in uses subtle green, Shared out remains turquoise.
- Patched `app/js/app.js`: team page Unlimited/Unlimited Team pills use CTA gradient `linear-gradient(135deg,#50CACC,#1A4FD6)` with white text.
- Verification: `node --check js/jumps.js`, `node --check js/app.js`, and `npm test` passed (28/28).

## JumpKit Configure Columns sharing pill icon removal — 2026-06-25
- Jeff asked to remove the icons before Shared out / Shared in in Configure Columns sharing pills.
- Patched `app/js/jumps.js` to remove the leading Tabler icons while keeping `Shared out → Team` and `Shared in ← Team`, colors, ellipsis, and tooltips.
- Verification: `node --check js/jumps.js` and `npm test` passed (28/28).

## JumpKit Teams page Unlimited Team gradient — 2026-06-25
- Jeff clarified the Teams page Unlimited Team pills still needed the CTA gradient.
- Patched `app/js/teams.js` owned and joined team `_ownedTierPill` / `_joinedTierPill` Unlimited Team pills to use `linear-gradient(135deg,#50CACC,#1A4FD6)` with white text.
- Verification: `node --check js/teams.js` and `npm test` passed (28/28).

## JumpKit Unlimited Team pill weight — 2026-06-25
- Jeff asked to unbold the Unlimited Team pill text.
- Patched `app/js/teams.js` Teams page owned/joined Unlimited Team pills and `app/js/app.js` home team summary Unlimited/Unlimited Team gradient pills from `font-weight:700` to `font-weight:500`.
- Verification: `node --check js/teams.js`, `node --check js/app.js`, and `npm test` passed (28/28).

## JumpKit Unlimited pill edge + Home team-card hover — 2026-06-25
- Jeff noticed pixelation on the left edge of the Unlimited Team gradient pill and asked that unlimited team cards on Home use the same hover styling as other cards.
- Patched `app/js/teams.js` and `app/js/app.js` Unlimited/Unlimited Team gradient pills: removed transparent border, added subtle inset white outline, `background-clip:padding-box`, `overflow:hidden`, and font smoothing to reduce edge artifacting.
- Patched `app/css/app.css` with `.home-dash .home-team-card:hover` using the same hover border/translate/shadow and `!important` border-color so inline team-card border styles don't block hover.
- Verification: `node --check js/teams.js`, `node --check js/app.js`, and `npm test` passed (28/28).

## JumpKit Unlimited pill weight tweak — 2026-06-25
- Jeff asked to make the text on the Unlimited card/pill slightly more bold after prior unbolding.
- Patched `app/js/teams.js` and `app/js/app.js` Unlimited/Unlimited Team gradient pill font weights from 500 to 600 (less heavy than the earlier 700).
- Verification: `node --check js/teams.js`, `node --check js/app.js`, and `npm test` passed (28/28).

## JumpKit Home Unlimited Team label — 2026-06-25
- Jeff asked Home page unlimited pills to say “Unlimited Team”.
- Patched `app/js/app.js` home team summary small tier pill label from `Unlimited` to `Unlimited Team` and matched free label to `Free Team` for consistency.
- Verification: `node --check js/app.js` and `npm test` passed (28/28).

## JumpKit Home page pill sizing — 2026-06-25
- Jeff asked to make the Home page pills 5% bigger.
- Patched `app/js/app.js` Home team summary pills: tier pills, role pills, paused pill, and shared column count pills increased roughly 5% in font size/padding/radius.
- Verification: `node --check js/app.js` and `npm test` passed (28/28).

## JumpKit Manage Testing clear-session stale state fix — 2026-06-25
- Jeff reported: after Clear Session → Yes, Clear Session, Manage Testing re-rendered as reset, but clicking Start New Testing Session still showed Replace Active Session.
- Root cause likely stale/live `jk_deploy_config.version` being read by the Start New Session click handler even after the modal had re-rendered in no-session mode. The handler checked live `_getReleaseState()?.version`, not the modal-open state.
- Patched `app/js/tests.js`: added `_clearReleaseSessionState(preserve)` that removes `jk_deploy_config` first, then writes back only preserved non-session fields (currently folder), preventing leftover version/results fields. Updated Clear Session confirm to use it.
- Patched Start New Session confirmation guard to only show Replace Active Session if the modal was opened with an active session (`existing?.version`) and live state still has a version. A no-session modal now starts directly even if stale async state briefly reappears.
- Verification: `node --check js/tests.js` and `npm test` passed (28/28).

## JumpKit Testing Save Results toast feedback — 2026-06-25
- Jeff reported clicking Save Results on Testing page no longer showed either success or error toast.
- Patched `app/js/tests.js`: Save Results buttons now pass their button element to `_saveReleaseSection`, temporarily show a disabled `Saving…` state, and restore afterward.
- Added `_releaseSaveToast(type,msg)` helper and replaced silent/alert-only branches with explicit toasts: no active session, no file I/O, no file picker, folder selection canceled, write failure, exceptions, and success.
- Success toast now says e.g. `Pre-Flight results saved successfully.`; failure toast starts `Save failed: ...`.
- Verification: `node --check js/tests.js` and `npm test` passed (28/28).

## JumpKit Testing Save Results no-op root fix — 2026-06-25
- Jeff reported Save Results still showed no toast and did not save anything.
- Found likely root cause: Save Results used direct click listeners attached during `renderTests()`. Clear Session rebuilt only the table via `_buildTestRows()`, replacing the Save Results buttons without re-attaching direct listeners. That made Save Results a no-op after reset/re-render.
- Patched `app/js/tests.js`: moved Save Results handling into the existing delegated document click handler `_testsJaction`, keyed by save button IDs, so rebuilt buttons continue to work without explicit re-wiring.
- Removed the direct Save Results listeners from `renderTests()` to avoid duplicate saves.
- Also declared `visibleCols` in both testing environment summary builders to avoid an accidental global/runtime fragility.
- Verification: `node --check js/tests.js` and `npm test` passed (28/28).

## JumpKit localStorage/sessionStorage audit fixes — 2026-06-25
- Jeff approved fixes for the three storage audit concerns: move `jk_recovery_snapshot_{userId}` out of localStorage, clear `jk_pending_upgrade_applied` on logout, and implement Option A for Supabase auth token storage.
- Implemented recovery snapshots in SQLite: added `recovery_snapshots` table and IPC handlers `save-recovery-snapshot`, `get-recovery-snapshot`, `delete-recovery-snapshot`; exposed them in preload; changed `DB.saveRecoverySnapshot()` / `_restoreRecoverySnapshot()` to use IPC/SQLite instead of localStorage. Browser/no-SQLite mode now deliberately avoids localStorage recovery snapshots because snapshots can contain jump URLs and click history.
- Updated logout/session flows to clear both `jk_session_token` and `jk_pending_upgrade_applied`; made snapshot saves awaited before manual/forced logout redirects and after import persistence.
- Implemented Electron secure Supabase auth persistence: `app/supabase/client.js` now supplies a custom Supabase auth storage adapter backed by Electron `safeStorage` via IPC (`secure-auth-get/set/remove`) and a userData `secure-auth-store.json` file containing encrypted values only. Legacy Supabase localStorage token for the active key is migrated into secure storage and removed once migration succeeds.
- Residual concern: browser/no-Electron mode no longer has persistent Supabase auth storage; in Electron release/dev builds safeStorage should preserve login. Manually verify login persistence after app restart and logout clears auth/session state.
- Verification: `node --check` passed for `main.js`, `preload.js`, `js/db.js`, `js/app.js`, `js/auth.js`, and `supabase/client.js`; `npm test` passed (28/28).

## JumpKit Automatic Tests run button skip cleanup — 2026-06-25
- Jeff reported some unit tests were skipped when clicking Run in the Automatic Tests section.
- Found `_runTests('auto')` included any non-manual/non-auto+manual test, which accidentally included preflight tests. That meant preflight-only tests like #380 could be pulled into the Automatic section run path even though they are not in the Automatic Tests section.
- Patched `_runTests()` to run exactly the current section's tests: Automatic excludes `preflight`, `[MANUAL]`, `[AUTO+MANUAL]`, and platform-inapplicable tests; Auto+Manual excludes `preflight` and platform-inapplicable tests.
- Added explicit `result === 'skip'` handling and skipped count in the text results writer, so intentional skips are tracked instead of misclassified.
- Changed automatic test #23 (Admin page render) so packaged builds pass as “admin.js intentionally excluded” instead of returning `skip`; the true admin-file exclusion is covered by preflight tests.
- Verification: script counted 132 automatic-section tests and 0 automatic tests returning `skip`; `node --check js/tests.js` and `npm test` passed (28/28).

## JumpKit DB audit change plan — 2026-06-25
- Jeff accepted audit item #6 (auto-backup behavior) as-is for now and asked to skip item #2 (dev/prod Supabase separation) for now.
- Added TodoKit JumpKit task `auto_db_audit_y2y1yog2` titled "Fix database audit release blockers" with no code implementation.
- Plan covers: untrack secrets/temp files + rotate Apple password if pushed, sanitize logs, tighten SQLite IPC scoping, and whitelist SQLite migrations.

## JumpKit DB audit blockers implemented — 2026-06-25
- Implemented approved DB audit items 1-4: ignored/untracked Supabase `.temp`, sanitized selected sensitive logs, whitelisted SQLite migrations, and tightened SQLite IPC scoping for shared-jump delete, click-log backfill, and sync state.
- `app/.env` is not tracked in current git history (`git log --all -- app/.env` returned no commits); Supabase `.temp/pooler-url` has commit history and is now staged for removal from git tracking while local files remain.
- Verification: `npm test` in `Topics/JumpKit/app` passed 28/28.
## JumpKit DB audit and release blocker fixes — 2026-06-25
- Jeff asked for a full JumpKit database audit covering local SQLite (`app/js/db.js`, `app/preload.js`, Electron IPC in `app/main.js`) and Supabase/Postgres (`app/supabase/`, Edge Functions). Audit result: mostly sound, but release blockers found around tracked secret/temp files, sensitive logging, SQLite IPC scoping, and dynamic migration SQL. Jeff said to skip dev/prod Supabase separation for now and accept backup behavior as-is.
- Added JumpKit TodoKit task `auto_db_audit_y2y1yog2` (“Fix database audit release blockers”) with a plan; later completed it after Jeff approved items 1-4.
- Implemented fixes: added ignore rules for `app/.env`, `.env.*`, and `app/supabase/.temp/`; untracked Supabase temp files with `git rm --cached` while keeping local copies; confirmed `app/.env` had no git history in current repo so no Apple password rotation was performed.
- Sanitized selected logs to avoid emails/profile IDs/team IDs in console/Edge Function output.
- Replaced dynamic SQLite migration identifier interpolation with whitelisted/fixed migrations.
- Tightened SQLite IPC scoping: `delete-shared-jumps` and `log-click-name` now require `userId`; `sync_state` supports user-scoped keys with legacy fallback.
- Verification after DB blocker fixes: JS syntax checks passed for touched JS files; `npm test` passed 28/28. Caveat: shared/team sync UI still should be manually checked because IPC scoping touched runtime behavior.
- TodoKit task `auto_db_audit_y2y1yog2` marked Completed with comment: “Implemented DB audit blockers 1-4; npm test passes. Jeff to check.”

## JumpKit Home jump count discrepancy — 2026-06-25
- Jeff saw Home Jumps card report 46 while visible columns summed to 41.
- SQLite inspection found active counts: 41 valid-column jumps + 10 orphan shared/team jumps whose `columnId` pointed to missing local columns (`cc07895b-f584-4122-a0fd-7882e9af54b9`, `de550b04-0238-4bb6-9f93-1fa42f4367fc`). The live app likely had one 5-jump orphan batch in memory, explaining 46.
- Made backup `/Users/jeffroder/Library/Application Support/jumpkit/jumpkit.db.pre-orphan-cleanup-20260625-083928` before cleanup.
- One-time SQLite cleanup removed orphan active shared/team jumps; final active count for user `6c0763f4-21d4-470a-bc10-1e6eafa3873a` is 41 and orphan count is 0.
- Code guard added: `DB._removeOrphanSharedJumps(userId)` removes active orphan shared/team jumps during init; `DB.getRenderableActiveJumps(userId)` counts only active jumps with valid local columns; Home card now uses renderable active jumps.
- Verification: `node --check js/db.js`, `node --check js/app.js`, and `npm test` passed (28/28).

## JumpKit orphan jump prevention audit — 2026-06-25
- Jeff suspected backup import could create orphan jumps. Checked import path plus shared-sync and SQLite IPC paths.
- Import path already maps selected active backup jumps to local selected columns, but hardened it: archived import now only reuses a personal Archived column (not shared/team), and `doImport` skips if target column is missing.
- Added DB-level invariant guards in `DB.createJump` and `DB.updateJump`: any jump create/update with a missing/invalid `columnId` is blocked before cache/SQLite persistence.
- Shared sync was the more likely orphan source: renderer and IPC used `rj.shared_column_id` as a fallback columnId if no local column mapping existed. Fixed sync to skip remote shared jumps unless the Supabase shared column maps to a real local column, and changed IPC `upsert-shared-jumps` to verify `(columnId,userId)` exists before SQLite upsert.
- Preserved `supabaseId` on IPC shared-jump upsert so stale deletion can match remote IDs reliably.
- Verification: syntax checks passed for `app/main.js`, `app/js/db.js`, `app/js/sync.js`, `app/js/app.js`; `npm test` from `Topics/JumpKit/app` passed 28/28; current SQLite orphan count remains 0.

## Pre-compaction flush — JumpKit DB/orphan jump work
- Jeff reported Home page Jumps card showing 46/51 active jumps while visible columns totaled 41. Investigation found local SQLite had orphan active shared/team jumps whose `columnId` referenced missing local columns.
- Cleaned local DB after backup: `/Users/jeffroder/Library/Application Support/jumpkit/jumpkit.db.pre-orphan-cleanup-20260625-083928`. Verified after cleanup: 41 active jumps, 0 active orphan jumps.
- Added app guards/fixes to prevent recurrence:
  - `DB._removeOrphanSharedJumps(userId)` cleanup for active shared/team jumps with missing columns.
  - Home card now uses renderable active jumps only via `DB.getRenderableActiveJumps()`.
  - `DB.createJump()` blocks invalid/missing `columnId`; `DB.updateJump()` blocks moving jumps to invalid/missing `columnId`.
  - Shared sync now skips remote shared jumps unless shared column maps to a real local column.
  - SQLite IPC `upsert-shared-jumps` verifies target `(columnId,userId)` exists before writing and preserves `supabaseId` for stale cleanup.
  - Backup import hardened: archived import only reuses a personal Archived column; import skips a jump if target column missing.
- Verified syntax checks passed for `app/main.js`, `app/js/db.js`, `app/js/sync.js`, `app/js/app.js`; test suite passed 28/28; SQLite orphan count 0.
- Jeff should refresh/restart JumpKit to load runtime guards.

## JumpKit Jumps page subtitle polish — 2026-06-25
- Jeff asked to update the subtle help text at the top of the Jumps page to include archived jump count and replace “clicks” with “launches.”
- Patched `app/js/jumps.js:getStatsText()` active-filter text to show `N active · N archived · N launches · N min saved`, using `DB.getRenderableActiveJumps()` when available.
- Patched `app/js/app.js:getStatsHTML()` for consistency: active stats summary includes archived count and uses renderable active jumps.
- Verification: `node --check js/jumps.js`, `node --check js/app.js`, and `npm test` passed (28/28).

## JumpKit stats/configure columns polish — 2026-06-25
- Jeff requested three UI fixes: stats page total time saved decimal places match Jumps page subtitle, Configure Columns show number of jumps per column, and personal columns show read-only Sharing value “Not shared” instead of looking editable.
- Patched `app/js/app.js:renderStatsDash()` time saved cards to show one decimal place (`toFixed(1)`) instead of rounded integer minutes.
- Patched `app/js/jumps.js:_makeColConfigRow()` to add a discrete Jumps count column and change personal sharing status to a read-only “Not shared” value.
- Patched `app/css/app.css` column config grid and added `.col-readonly-value` / `.col-jump-count` styles.
- Verification: `node --check js/app.js`, `node --check js/jumps.js`, and `npm test` passed (28/28).

## JumpKit Configure Columns modal width/alignment polish — 2026-06-25
- Jeff requested Configure Columns modal 15% wider, stable horizontal alignment inside drag/drop cards, ellipsis for long sharing team names, and “Not shared” directly under SHARING.
- Patched `app/js/jumps.js` to open Configure Columns with `lg config-cols-modal`, wrap team names in `.col-status-text` for ellipsis, and render personal sharing as plain read-only “Not shared” text (no icon offset).
- Patched `app/css/app.css`: `.modal-box.config-cols-modal { max-width: 782px; }` (15% wider than 680px lg), grid uses `minmax(0,...)`, sharing field clips overflow, and `.col-status-text` ellipsizes long names.
- Verification: `node --check js/jumps.js` and `npm test` passed (28/28).

## JumpKit Configure Columns sharing direction badges — 2026-06-25
- Jeff approved adding clearer sharing direction in Configure Columns.
- Patched `app/js/jumps.js` sharing badges to show `Shared out · Team Name` for owned/team-owner shared columns and `Shared in · Team Name` for columns shared into JumpKit as a team member. Added directional Tabler icons and explanatory tooltips.
- Patched `app/css/app.css` with `.col-status-dir`, `.col-status-dot`, and stronger shared-out tint while preserving ellipsis on team names.
- Verification: `node --check js/jumps.js` and `npm test` passed (28/28).

## JumpKit sharing pill and team pill polish — 2026-06-25
- Jeff requested Configure Columns sharing pills: less left padding before Shared out/in, use arrow separators instead of dot, unbold Shared out/in text, subtle green for Shared in to align with Member green, and recolor Team page Unlimited pill to CTA gradient.
- Patched `app/js/jumps.js`: pills now render `Shared out → Team Name` and `Shared in ← Team Name`.
- Patched `app/css/app.css`: tighter left padding, `.col-status-dir` unbolded, `.col-status-arrow` added, Shared in uses subtle green, Shared out remains turquoise.
- Patched `app/js/app.js`: team page Unlimited/Unlimited Team pills use CTA gradient `linear-gradient(135deg,#50CACC,#1A4FD6)` with white text.
- Verification: `node --check js/jumps.js`, `node --check js/app.js`, and `npm test` passed (28/28).

## JumpKit Configure Columns sharing pill icon removal — 2026-06-25
- Jeff asked to remove the icons before Shared out / Shared in in Configure Columns sharing pills.
- Patched `app/js/jumps.js` to remove the leading Tabler icons while keeping `Shared out → Team` and `Shared in ← Team`, colors, ellipsis, and tooltips.
- Verification: `node --check js/jumps.js` and `npm test` passed (28/28).

## JumpKit Teams page Unlimited Team gradient — 2026-06-25
- Jeff clarified the Teams page Unlimited Team pills still needed the CTA gradient.
- Patched `app/js/teams.js` owned and joined team `_ownedTierPill` / `_joinedTierPill` Unlimited Team pills to use `linear-gradient(135deg,#50CACC,#1A4FD6)` with white text.
- Verification: `node --check js/teams.js` and `npm test` passed (28/28).

## JumpKit Unlimited Team pill weight — 2026-06-25
- Jeff asked to unbold the Unlimited Team pill text.
- Patched `app/js/teams.js` Teams page owned/joined Unlimited Team pills and `app/js/app.js` home team summary Unlimited/Unlimited Team gradient pills from `font-weight:700` to `font-weight:500`.
- Verification: `node --check js/teams.js`, `node --check js/app.js`, and `npm test` passed (28/28).

## JumpKit Unlimited pill edge + Home team-card hover — 2026-06-25
- Jeff noticed pixelation on the left edge of the Unlimited Team gradient pill and asked that unlimited team cards on Home use the same hover styling as other cards.
- Patched `app/js/teams.js` and `app/js/app.js` Unlimited/Unlimited Team gradient pills: removed transparent border, added subtle inset white outline, `background-clip:padding-box`, `overflow:hidden`, and font smoothing to reduce edge artifacting.
- Patched `app/css/app.css` with `.home-dash .home-team-card:hover` using the same hover border/translate/shadow and `!important` border-color so inline team-card border styles don't block hover.
- Verification: `node --check js/teams.js`, `node --check js/app.js`, and `npm test` passed (28/28).

## JumpKit Unlimited pill weight tweak — 2026-06-25
- Jeff asked to make the text on the Unlimited card/pill slightly more bold after prior unbolding.
- Patched `app/js/teams.js` and `app/js/app.js` Unlimited/Unlimited Team gradient pill font weights from 500 to 600 (less heavy than the earlier 700).
- Verification: `node --check js/teams.js`, `node --check js/app.js`, and `npm test` passed (28/28).

## JumpKit Home Unlimited Team label — 2026-06-25
- Jeff asked Home page unlimited pills to say “Unlimited Team”.
- Patched `app/js/app.js` home team summary small tier pill label from `Unlimited` to `Unlimited Team` and matched free label to `Free Team` for consistency.
- Verification: `node --check js/app.js` and `npm test` passed (28/28).

## JumpKit Home page pill sizing — 2026-06-25
- Jeff asked to make the Home page pills 5% bigger.
- Patched `app/js/app.js` Home team summary pills: tier pills, role pills, paused pill, and shared column count pills increased roughly 5% in font size/padding/radius.
- Verification: `node --check js/app.js` and `npm test` passed (28/28).

## JumpKit Manage Testing clear-session stale state fix — 2026-06-25
- Jeff reported: after Clear Session → Yes, Clear Session, Manage Testing re-rendered as reset, but clicking Start New Testing Session still showed Replace Active Session.
- Root cause likely stale/live `jk_deploy_config.version` being read by the Start New Session click handler even after the modal had re-rendered in no-session mode. The handler checked live `_getReleaseState()?.version`, not the modal-open state.
- Patched `app/js/tests.js`: added `_clearReleaseSessionState(preserve)` that removes `jk_deploy_config` first, then writes back only preserved non-session fields (currently folder), preventing leftover version/results fields. Updated Clear Session confirm to use it.
- Patched Start New Session confirmation guard to only show Replace Active Session if the modal was opened with an active session (`existing?.version`) and live state still has a version. A no-session modal now starts directly even if stale async state briefly reappears.
- Verification: `node --check js/tests.js` and `npm test` passed (28/28).

## JumpKit Testing Save Results toast feedback — 2026-06-25
- Jeff reported clicking Save Results on Testing page no longer showed either success or error toast.
- Patched `app/js/tests.js`: Save Results buttons now pass their button element to `_saveReleaseSection`, temporarily show a disabled `Saving…` state, and restore afterward.
- Added `_releaseSaveToast(type,msg)` helper and replaced silent/alert-only branches with explicit toasts: no active session, no file I/O, no file picker, folder selection canceled, write failure, exceptions, and success.
- Success toast now says e.g. `Pre-Flight results saved successfully.`; failure toast starts `Save failed: ...`.
- Verification: `node --check js/tests.js` and `npm test` passed (28/28).

## JumpKit Testing Save Results no-op root fix — 2026-06-25
- Jeff reported Save Results still showed no toast and did not save anything.
- Found likely root cause: Save Results used direct click listeners attached during `renderTests()`. Clear Session rebuilt only the table via `_buildTestRows()`, replacing the Save Results buttons without re-attaching direct listeners. That made Save Results a no-op after reset/re-render.
- Patched `app/js/tests.js`: moved Save Results handling into the existing delegated document click handler `_testsJaction`, keyed by save button IDs, so rebuilt buttons continue to work without explicit re-wiring.
- Removed the direct Save Results listeners from `renderTests()` to avoid duplicate saves.
- Also declared `visibleCols` in both testing environment summary builders to avoid an accidental global/runtime fragility.
- Verification: `node --check js/tests.js` and `npm test` passed (28/28).

## JumpKit localStorage/sessionStorage audit fixes — 2026-06-25
- Jeff approved fixes for the three storage audit concerns: move `jk_recovery_snapshot_{userId}` out of localStorage, clear `jk_pending_upgrade_applied` on logout, and implement Option A for Supabase auth token storage.
- Implemented recovery snapshots in SQLite: added `recovery_snapshots` table and IPC handlers `save-recovery-snapshot`, `get-recovery-snapshot`, `delete-recovery-snapshot`; exposed them in preload; changed `DB.saveRecoverySnapshot()` / `_restoreRecoverySnapshot()` to use IPC/SQLite instead of localStorage. Browser/no-SQLite mode now deliberately avoids localStorage recovery snapshots because snapshots can contain jump URLs and click history.
- Updated logout/session flows to clear both `jk_session_token` and `jk_pending_upgrade_applied`; made snapshot saves awaited before manual/forced logout redirects and after import persistence.
- Implemented Electron secure Supabase auth persistence: `app/supabase/client.js` now supplies a custom Supabase auth storage adapter backed by Electron `safeStorage` via IPC (`secure-auth-get/set/remove`) and a userData `secure-auth-store.json` file containing encrypted values only. Legacy Supabase localStorage token for the active key is migrated into secure storage and removed once migration succeeds.
- Residual concern: browser/no-Electron mode no longer has persistent Supabase auth storage; in Electron release/dev builds safeStorage should preserve login. Manually verify login persistence after app restart and logout clears auth/session state.
- Verification: `node --check` passed for `main.js`, `preload.js`, `js/db.js`, `js/app.js`, `js/auth.js`, and `supabase/client.js`; `npm test` passed (28/28).

## JumpKit Automatic Tests run button skip cleanup — 2026-06-25
- Jeff reported some unit tests were skipped when clicking Run in the Automatic Tests section.
- Found `_runTests('auto')` included any non-manual/non-auto+manual test, which accidentally included preflight tests. That meant preflight-only tests like #380 could be pulled into the Automatic section run path even though they are not in the Automatic Tests section.
- Patched `_runTests()` to run exactly the current section's tests: Automatic excludes `preflight`, `[MANUAL]`, `[AUTO+MANUAL]`, and platform-inapplicable tests; Auto+Manual excludes `preflight` and platform-inapplicable tests.
- Added explicit `result === 'skip'` handling and skipped count in the text results writer, so intentional skips are tracked instead of misclassified.
- Changed automatic test #23 (Admin page render) so packaged builds pass as “admin.js intentionally excluded” instead of returning `skip`; the true admin-file exclusion is covered by preflight tests.
- Verification: script counted 132 automatic-section tests and 0 automatic tests returning `skip`; `node --check js/tests.js` and `npm test` passed (28/28).

## JumpKit code quality audit — 2026-06-25
- Jeff asked for a full code quality audit of `Topics/JumpKit/app/js/*.js` excluding `chart.min.js`, with PASS/FAIL/N/A and line-number evidence for FAILs, and explicitly said not to make changes until proposed changes are confirmed.
- Audit findings reported without code changes: PASS for no `console.debug()`, no commented-out console.log debug blocks, no `.bak/.tmp/.orig` junk files in `app/js`, no release-blocking TODO/FIXME/HACK/XXX comments, no obvious hardcoded production test emails/user IDs outside test/help contexts, no disabled auth guards, and mostly idempotent event listeners.
- FAILs reported: production `console.log()` calls in `app/js/tests.js`; async UI flows lacking try/catch/finally (`jumps.js:saveColumns`, deployment save handler); fetch calls without `response.ok` checks (`auth.js` Edge Function emails, logo fetch in `app.js`); Supabase calls ignoring `{ error }` in auth/teams/app/deployment paths; Edge Function failures swallowed or falsely shown as success; missing loading/error states and possible stuck loading buttons; unbounded Supabase list queries; click log loaded wholesale into memory; unsanitized `innerHTML` interpolation/XSS risk in auth toast/modal, app Toast, jumps column selector, teams modals; session polling timer lacks lifecycle cleanup/max guard.
- Important: no fixes have been applied for this code quality audit yet. If Jeff approves, likely start with low-risk release blockers: remove `console.log`, centralize safe HTML escaping/toast textContent, wrap save/deployment async handlers, add fetch/Supabase error checks, add query limits, and clear polling interval on logout/pagehide.

## JumpKit code quality audit fixes — 2026-06-25
- Jeff approved fixing all code quality audit failures from the `app/js/*.js` scan excluding `chart.min.js`.
- Implemented targeted fixes: removed remaining executable `console.log` traces and reworded in-app audit prompt text so simple grep scans no longer false-positive on console/TODO marker strings; no `console.log`/`console.debug`, commented console debug blocks, `select('*')`, or TODO/FIXME/HACK/XXX hits remain in `app/js/*.js` excluding `chart.min.js`.
- Hardened async/UI flows: `saveColumns()` now has outer try/catch and restores button state on failure; deployment Manage modal save/folder paths now use loading/error states and check Supabase errors; invite/send flows now surface failures instead of silently swallowing user-initiated Edge Function errors.
- Hardened fetch/Supabase handling: auth Edge Function helper checks `response.ok` before success-body parsing; feedback submit checks non-2xx and shows user-visible failure instead of false success; logo fetch checks `ok`; selected profile/session/team/invite/deployment queries now check returned errors.
- Added bounds/reduced unbounded reads: deployment list queries have limits; shared sync `shared_columns/shared_jumps` have limits and explicit selected columns; team/invite/membership queries have explicit selected columns/limits; SQLite `get-click-log` returns latest 10k rows ordered ascending.
- Patched XSS-prone interpolation: Toast now inserts message text via `textContent`; signup/session conflict/deployment/admin/jumps/team removal/sync stale-column/team ROI strings are escaped where user-controlled values enter HTML.
- Added session lock interval cleanup on pagehide/beforeunload/logout and before creating a new watcher.
- Verification: `node --check` passed for touched JS/main files; `npm test` passed 28/28; final grep checks were clean for console log/debug calls, commented console debug blocks, backup junk files, select-star queries, and TODO/FIXME/HACK/XXX markers.

## JumpKit security audit fixes — 2026-06-25
- Jeff approved fixing all security audit findings (1 High, 5 Mediums, 2 Lows).
- High: rewrote `open-url` IPC in `app/main.js` with explicit blocked-scheme list (javascript, data, file, vbscript, smb, ssh, vnc, x-apple.systempreferences, prefs, ms-settings, etc.), allow-list for common deep-link schemes (mailto, slack, zoommtg, msteams, obsidian, etc.), strict absolute-path requirement for local jumps, and structured `{ok, reason}` returns. Protocol-relative `//` URLs rejected.
- Medium 1: added `sandbox: true` to hidden PDF export `BrowserWindow`.
- Medium 2: added `<meta http-equiv="Content-Security-Policy">` fallback to `app/index.html` and `app/app.html` matching the existing header-based CSP (covers `file://` loads where webRequest doesn't fire).
- Medium 3: verified team/owner names in PDF export and sync stale-team modal already use `esc()` / `syncEsc()` — no additional patches needed.
- Medium 4: `apply-pending-upgrade` Edge Function now calls `supabase.auth.getUser(jwt)` and verifies the JWT user email matches the requested email before applying any upgrade. Prevents authenticated users from triggering another user's pending upgrade.
- Medium 5: `send-feedback` Edge Function now enforces `message ≤ 5000 chars`, other fields `≤ 200 chars`, and validates email shape before sending.
- Medium 6: noted in audit only — `user_sessions` RLS requires server-side verification by Jeff (cannot inspect from client).
- Low: removed `homeDir` exposure from `app/preload.js` (no renderer usage found).
- Low: `express` kept in deps — `app/server.js` uses it but is excluded from production build; left intact to avoid breaking dev helpers.
- Verification: `node --check` passed for all touched JS/main files; `npm test` passed 28/28.

## JumpKit email templates overhaul — 2026-06-25
- Comprehensive styling/behavior pass across all 12 email Edge Functions over multiple rounds:
  - Logo sizing iterated to header 75px / footer 54px (was 50/36, +50% net).
  - Standardized `position:relative;left:6px` (left:12px feedback footer) for logo nudging.
  - Tagline "Stop searching. Start jumping." tightened with -15px top margin under logo.
  - send-welcome-core: ⚡ → 🎉; "What's included" card restyled to match send-cancellation card (bg, border, title #C8D6E8, text #7A93B4, icon-check-turquoise).
  - send-cancellation: "Reactivate Core" → "Reactivate Unlimited" (jumpkit-white icon kept after a brief rocket-icon detour).
  - send-team-downgrade-alert: CTAs got jumpkit-white icon; affected-member <ul> uses list-style:disc inside; padding-left:0 to align flush with paragraph text; font 14→15px line-height 1.7.
  - send-invite + waitlist-signup: wrapped with <!DOCTYPE html><html><head/></head><body style="..."> so bg/font match the other 10 emails.
  - send-feedback: logo path fixed (/img/ → / which was 404); header/footer normalized to 75/54 with identical styles to send-welcome; card margin 40px → 20px; tagline margins normalized.
  - send-account-exists: Sign In icon 27×27, padding tuned to 6px 19px so its height matches Reset Your Password (10px 19px, 18×18 icon).
  - send-member-joined / send-member-removed / send-team-deleted: card margin 40px → 20px (matches send-welcome).
  - All 7 CTA gradient buttons standardized to padding:10px 19px (after iterating -40% then +25%).
- All 12 functions redeployed to Supabase project iuexwdjnqfidcwvwbgwr; commit a30745a pushed to main.
- Local preview tooling lives at /tmp/jk-email-previews/ (render.mjs uses esbuild to transpile each Deno TS file with stubs for serve/createClient/Deno.env, then invokes buildXxxHTML with mock args, writes per-template HTML, and builds an index page; served via `python3 -m http.server 8722`).

## JumpKit tests/Teams page polish — 2026-06-25
- Teams page error "column team_invites.created_at does not exist": dropped non-existent column from select in app/js/teams.js line 194.
- Test #69 (and my _ensureTestTeam helper) were demoting admin → org-owner; guarded both to never demote admin. After running the broken code, restored Jeff's profile.role='admin' via Supabase SQL editor.
- Made tests #70/#71/#73 self-healing via new _ensureTestTeam() helper that lazy-creates a test team and self-heals on stale/wrong-owner _testTeamId.
- Added _ensureTestSharedColumn() helper; tests #78/#79/#80/#81/#82/#83 now route through user-owned test team so RLS is_team_owner() passes (was failing with "row violates row-level security policy for table shared_jumps").
- Test #374 cleanup confirmed: purges all __jk_test_team_* + __TEST_* artifacts. Exec order #108 in Run-All sequence.
- Pre-Flight section icon `flag` was missing from minified Tabler sprite; swapped to `checklist`. Audited all js/css icon refs — `flag` was the only ghost.
