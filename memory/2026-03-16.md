# Memory — 2026-03-16

## JumpKit Landing Page — Morning Session

### Fixes applied (all deployed to jumpkit.app via Vercel):

1. **Stray SVG path data removed** from Instant Jump Launcher feature card — raw `<path d="..."/>` text was sitting outside any `<svg>` tag, breaking the card title visually. Fixed by stripping the path data.
2. **Jump icon still broken** — `.jump-icon-inline` span is in the HTML but the icon is not rendering on the live site. Root cause not yet fully confirmed — likely a CSS mask URL path issue (`url(assets/jump-icon.svg)` may not resolve correctly from Vercel's CDN). **Still needs a real fix.**
3. **Logging for Audits card** — added to Jet AI section, then removed at Jeff's request. Currently present again in HTML (5th card), re-added after Jeff specified exact Jet AI card order.
4. **Jet AI card order** (as of last deploy): Agentic AI → Zero Data Leakage → Office Automation → Logging for Audits → Industries. Jeff confirmed this is the desired order.
5. **ROI chart** — removed target info (/ 5h column), bars now scale by ROI value using inline `width` %, labels show "Xh · $Y" to the right of each bar.
6. **Jeff asked to undo last change** (8:39am) — "last change" was the Jet AI grid reorder. Was mid-execution when session compacted.

### Current HTML state (Jet AI grid order in source):
1. Agentic AI, Locally Run
2. Zero Data Leakage
3. Office Automation (was Industries — swapped multiple times this session)
4. Logging for Audits (Immutable Audit Trail in source)
5. Industries

### Known issues still open:
- Jump icon (`.jump-icon-inline`) not rendering on live site
- "Undo last change" request from Jeff at 8:39am may not have been fully applied before compaction
- No git history in landing folder — no clean rollback mechanism; changes are manual

### Key file paths:
- Landing source: `/Users/jeffroder/.openclaw/workspace/Topics/JumpKit/landing/index.html`
- CSS: `Topics/JumpKit/landing/style.css`
- Deploy token: `vcp_1u55cbpSbb1E8Dt7eOw5jJUw1KZRzti9nnxHl3y09vWMWh1S5p2Y344B`
- Deploy cmd: `vercel deploy --prod --token <token>` from landing folder
